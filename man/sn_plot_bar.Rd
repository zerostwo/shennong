% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_composition.R
\name{sn_plot_bar}
\alias{sn_plot_bar}
\title{Plot composition bars with string column names}
\usage{
sn_plot_bar(
  data,
  x_by,
  fill_by,
  y_by = NULL,
  panel_widths = NULL,
  panel_heights = NULL,
  ...
)
}
\arguments{
\item{data}{A Seurat object or metadata/summary data frame.}

\item{x_by}{Column name defining the x axis.}

\item{fill_by}{Column name string for the composition category.}

\item{y_by}{Optional summarized value column name.}

\item{panel_widths, panel_heights}{Optional positive numeric panel dimensions
in points (pt), applied directly through ggplot2 for every composition type.
Scalars repeat across facets; vectors specify facet column widths or row
heights. Style C accepts scalars only to preserve circular pies. If only
one dimension and \code{aspect_ratio} are supplied, the other is derived; C
derives it from its coordinate ratio even when \code{aspect_ratio} is omitted.
Explicit width and height take precedence over \code{aspect_ratio}. These sizes
exclude outer labels/margins; choose a sufficiently large export canvas.}

\item{...}{Additional named arguments to \code{\link[=sn_plot_composition]{sn_plot_composition()}}, including
\code{sankey_layout}, \code{stratum_gap}, \code{show_stratum_boxes}, \code{stratum_label_size},
\code{show_pies}, \code{node_palette}, \code{axis_labels}, \code{facet_row_by}, \code{facet_col_by},
\code{y_by} for summarized weights, and \code{data_kind}.}
}
\value{
A ggplot with Shennong figure metadata.
}
\description{
Plot composition bars with string column names
}
\examples{
d <- data.frame(sample = c("S1", "S1", "S2"), celltype = c("T", "B", "T"))
sn_plot_bar(d, x_by = "sample", fill_by = "celltype")
}
