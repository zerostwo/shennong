% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{sn_plot_dot}
\alias{sn_plot_dot}
\title{Plot a dot plot with categorical groups}
\usage{
sn_plot_dot(
  x,
  assay = NULL,
  features,
  de_name = "default",
  n = 3,
  marker_groups = NULL,
  col_min = -2.5,
  col_max = 2.5,
  dot_min = 0,
  dot_scale = 4,
  idents = NULL,
  group_by = NULL,
  split_by = NULL,
  cluster_idents = FALSE,
  scale = TRUE,
  scale_by = "radius",
  scale_min = NA,
  scale_max = NA,
  palette = "RdBu",
  direction = 1,
  panel_widths = NULL,
  panel_heights = NULL,
  title = NULL,
  x_label = NULL,
  y_label = NULL
)
}
\arguments{
\item{x}{A Seurat object containing the data to plot.}

\item{assay}{The assay to plot. Defaults to the default assay.}

\item{features}{A character vector of feature names to plot, or
\code{"top_markers"} to automatically use the top stored DE markers from
\code{object@misc$de_results[[de_name]]}.}

\item{de_name}{Name of the stored DE result to use when
\code{features = "top_markers"}. Defaults to \code{"default"}.}

\item{n}{Number of genes to select per group when
\code{features = "top_markers"}. Defaults to \code{3}.}

\item{marker_groups}{Optional subset of DE result groups to include when
\code{features = "top_markers"}.}

\item{col_min}{The minimum value for the color scale. Defaults to -2.5.}

\item{col_max}{The maximum value for the color scale. Defaults to 2.5.}

\item{dot_min}{The minimum value for the dot size scale. Defaults to 0.}

\item{dot_scale}{The size of the dots to plot.}

\item{idents}{A character vector of identities to plot. Defaults to all identities.}

\item{group_by}{A character vector specifying the grouping variable. Defaults to "ident".}

\item{split_by}{A character vector specifying the splitting variable.}

\item{cluster_idents}{Whether to cluster the identities or not. Defaults to FALSE.}

\item{scale}{Whether to scale the dot size or not. Defaults to TRUE.}

\item{scale_by}{The variable to scale the dot size by. Defaults to "radius".}

\item{scale_min}{The minimum value for the dot size scale. Defaults to NA.}

\item{scale_max}{The maximum value for the dot size scale. Defaults to NA.}

\item{palette}{The palette used for the continuous color scale.}

\item{direction}{Direction for the continuous palette. Use \code{1} for the
default order and \code{-1} to reverse it.}

\item{panel_widths, panel_heights}{Optional panel size arguments forwarded to
\code{catplot::theme_cat()} when available.}

\item{title}{Optional plot title.}

\item{x_label, y_label}{Optional axis labels.}
}
\value{
A ggplot2 object.
}
\description{
This function plots a dot plot with categorical groups using the DotPlot function from the Seurat package.
}
\examples{
\dontrun{
sn_plot_dot(x = mySeuratObject, features = c("CD3D", "CD8A", "CD4"))
}

}
