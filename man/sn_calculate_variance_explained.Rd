% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_metrics.R
\name{sn_calculate_variance_explained}
\alias{sn_calculate_variance_explained}
\title{Rank metadata variables by embedding variance explained}
\usage{
sn_calculate_variance_explained(
  x,
  variables,
  reduction = .sn_default_metric_reduction(x),
  dims = NULL,
  method = c("single", "partial"),
  cells = NULL,
  max_cells = NULL,
  stratify_by = NULL,
  seed = 717,
  return_dim_data = FALSE
)
}
\arguments{
\item{x}{A Seurat object.}

\item{variables}{Character vector of metadata columns to rank.}

\item{reduction}{Reduction name used for the score. Defaults to
\code{"harmony"} when present, otherwise \code{"pca"}.}

\item{dims}{Optional integer vector of embedding dimensions to retain.}

\item{method}{One of \code{"single"} or \code{"partial"}. Defaults to
\code{"single"}.}

\item{cells}{Optional character vector of cell names to include.}

\item{max_cells}{Optional integer cap used to subsample cells before running
the metric.}

\item{stratify_by}{Optional metadata column used to preserve representation
during subsampling.}

\item{seed}{Random seed used when \code{max_cells} triggers subsampling.}

\item{return_dim_data}{Logical; if \code{TRUE}, return a list containing the
summary table and per-dimension results.}
}
\value{
A data frame ranked by \code{variance_explained}. When
\code{return_dim_data = TRUE}, a list with \code{summary} and
\code{dim_data} is returned.
}
\description{
Quantifies how much variation in a dimensional reduction is explained by one
or more metadata variables. This is useful for identifying whether
\code{platform}, \code{study}, \code{tissue}, \code{sample}, or another
covariate is the dominant driver of residual batch structure.
}
\details{
Two modes are available. \code{method = "single"} fits one model per
variable and reports the weighted R-squared across the selected dimensions.
\code{method = "partial"} fits all variables together and reports the
incremental variance explained by each variable after the others. Partial
estimates are helpful but can be ambiguous when variables are nested or
confounded, such as one platform per study.
}
\examples{
\dontrun{
variance_tbl <- sn_calculate_variance_explained(
  seu,
  variables = c("platform", "study", "tissue", "sample"),
  reduction = "pca",
  dims = 1:30
)
variance_tbl
}

}
