% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/program_scoring.R
\name{sn_score_programs}
\alias{sn_score_programs}
\title{Score gene programs in cells or aggregated samples}
\usage{
sn_score_programs(
  object,
  signatures,
  method = c("ucell", "aucell", "gsva", "ssgsea", "mean"),
  assay = NULL,
  layer = "data",
  group_by = NULL,
  aggregate = NULL,
  species = NULL,
  min_genes = 1L,
  backend_control = list(),
  seed = 717,
  return_object = TRUE,
  result_id = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{signatures}{Named list, program/gene data frame, named gene vector, or
bundled signature query vector.}

\item{method}{Scoring backend. UCell is the default for per-cell data.}

\item{assay, layer}{Expression source.}

\item{group_by}{Optional metadata column identifying groups to aggregate.}

\item{aggregate}{Required with \code{group_by}: \code{"expression"} averages
expression before scoring, while \code{"scores"} averages per-cell scores
after scoring. These differ for nonlinear scoring methods.}

\item{species}{Species required for bundled signature queries.}

\item{min_genes}{Minimum matched features required per signature.}

\item{backend_control}{Named backend-specific control list, keyed by method.}

\item{seed}{Random seed applied locally during scoring. Use \code{NULL} to
use the caller's random state. Effective seeds are recorded in provenance.}

\item{return_object}{Return the updated object or the unified result.}

\item{result_id}{Stable identifier for the stored program-scoring result and
its metadata prefix. When omitted, a unique method-derived ID is allocated.}

\item{overwrite}{Explicitly replace an existing result. Requires an explicit
\code{result_id}; obsolete score columns owned by that result are removed.}
}
\value{
A Seurat object or unified program-scoring result.
}
\description{
Provides one stable interface for sparse-aware UCell, AUCell, GSVA, ssGSEA,
and dependency-free mean-expression scoring. Per-cell scores are added to
Seurat metadata; aggregated scores remain in the stored result.
Metadata names are unique across programs and existing columns. The stored
\code{tables$metadata_columns} maps each program to its metadata column.
}
\examples{
\dontrun{
object <- sn_score_programs(
  object,
  signatures = list(T_cell = c("CD3D", "CD3E")),
  method = "ucell",
  result_id = "immune_programs"
)
sn_get_result(object, "program_scoring", "immune_programs")
}

}
