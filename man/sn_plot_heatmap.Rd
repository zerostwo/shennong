% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{sn_plot_heatmap}
\alias{sn_plot_heatmap}
\title{Plot a heatmap for selected genes or features}
\usage{
sn_plot_heatmap(
  object,
  features,
  group_by = "ident",
  split_by = NULL,
  assay = NULL,
  layer = "scale.data",
  slot = NULL,
  mode = c("cells", "average"),
  scale_data = TRUE,
  max_cells = NULL,
  seed = 717,
  palette = "RdBu",
  direction = -1,
  disp_min = -2.5,
  disp_max = NULL,
  col_min = -2.5,
  col_max = 2.5,
  average_fun = c("mean", "median"),
  average_scale = TRUE,
  group_bar = TRUE,
  label = TRUE,
  raster = TRUE,
  label_size = 8,
  label_angle = 45,
  angle = NULL,
  group_palette = "Paired",
  group_colors = NULL,
  show_cell_names = FALSE,
  show_ticks = FALSE,
  show_legend = TRUE,
  title = NULL,
  collect_legend = TRUE,
  aspect_ratio = NULL,
  panel_widths = NULL,
  panel_heights = NULL,
  ...
)
}
\arguments{
\item{object}{A Seurat object.}

\item{features}{Character vector of genes or features to display.}

\item{group_by}{Metadata column used to order and label cells. Use
\code{NULL}, \code{""}, or \code{"ident"} for current Seurat identities.}

\item{split_by}{Optional metadata column. When supplied, one heatmap is drawn
for each split level and combined with patchwork.}

\item{assay}{Assay used for expression retrieval.}

\item{layer}{Data layer used for expression retrieval. Defaults to
\code{"scale.data"} for cell-level heatmaps. In \code{mode = "average"},
use \code{"data"} to average normalized expression before optional
feature-wise scaling.}

\item{slot}{Deprecated alias for \code{layer}.}

\item{mode}{One of \code{"cells"} or \code{"average"}. \code{"cells"}
displays cells; \code{"average"} averages expression by \code{group_by}
and optional \code{split_by}.}

\item{scale_data}{If \code{TRUE} and \code{layer = "scale.data"}, scale the
requested features when they are missing from the scaled layer. Used only
in \code{mode = "cells"}.}

\item{max_cells}{Optional maximum number of cells to show per cell-level
heatmap panel.}

\item{seed}{Optional seed used when \code{max_cells} downsamples cells.}

\item{palette}{Continuous color palette name or vector.}

\item{direction}{Palette direction.}

\item{disp_min, disp_max}{Display limits passed to
\code{Seurat::DoHeatmap()}.}

\item{col_min, col_max}{Display limits for \code{mode = "average"}.}

\item{average_fun}{Summary used in \code{mode = "average"}.}

\item{average_scale}{Whether to feature-wise z-score average expression in
\code{mode = "average"}.}

\item{group_bar, label, raster}{Passed to \code{Seurat::DoHeatmap()} for
\code{mode = "cells"}. Rasterization is enabled by default.}

\item{label_size, label_angle}{Size in points and rotation angle for the
cell-level group labels drawn above the heatmap.}

\item{angle}{Deprecated alias for \code{label_angle}.}

\item{group_palette, group_colors}{Palette or explicit colors for the
cell-level group label bar. Defaults to \code{"Paired"}.}

\item{show_cell_names, show_ticks}{Whether to show cell-level x-axis labels
and axis ticks in \code{mode = "cells"}. Both default to \code{FALSE}.}

\item{show_legend}{Whether to keep the heatmap legend.}

\item{title}{Optional plot title. When \code{split_by} is supplied, split
levels are used as panel titles unless \code{title} is also supplied.}

\item{collect_legend}{Whether to collect patchwork legends for split plots.}

\item{aspect_ratio, panel_widths, panel_heights}{Optional sizing controls
forwarded through the Shennong/catplot theme helper.}

\item{...}{Additional arguments passed to \code{Seurat::DoHeatmap()}.}
}
\value{
A ggplot2 or patchwork object.
}
\description{
\code{sn_plot_heatmap()} draws a compact heatmap for user-selected genes. In
\code{mode = "cells"} it renders cells with Seurat's heatmap backend, while
\code{mode = "average"} first averages expression within the requested
metadata groups and displays a group-level heatmap.
}
\examples{
\dontrun{
sn_plot_heatmap(
  seurat_obj,
  features = c("CD3D", "MS4A1", "LYZ"),
  group_by = "seurat_clusters"
)
}
}
