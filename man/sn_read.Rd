% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_io.R
\name{sn_read}
\alias{sn_read}
\alias{.import.rio_bpcells}
\alias{.import.rio_10x}
\alias{.import.rio_starsolo}
\alias{.import.rio_h5ad}
\alias{.import.rio_h5}
\alias{.import.rio_gmt}
\title{Read tabular and bioinformatics file formats}
\usage{
sn_read(path, format, to = NULL, which, row_names = NULL, ...)

.import.rio_bpcells(file, ...)

.import.rio_10x(file, ...)

.import.rio_starsolo(file, ...)

.import.rio_h5ad(file, ...)

.import.rio_h5(file, ...)

.import.rio_gmt(file, ...)
}
\arguments{
\item{path}{Path, URL, or supported directory to import.}

\item{format}{Optional explicit format override.}

\item{to}{Optional target class forwarded to \code{rio}.}

\item{which}{Optional archive member index for compressed archives.}

\item{row_names}{Optional column index or name to promote to row names when
importing a data frame.}

\item{...}{Additional arguments forwarded to the underlying importer.}

\item{file}{Input path used by the exported \code{rio} adapter methods.}
}
\value{
The imported object. Tabular inputs are typically returned as data
frames, while supported matrix-like formats are returned in their native
classes.

A data.frame with two columns: \code{term} and \code{gene}.
}
\description{
A Shennong wrapper around \code{rio::import()} with support for common
single-cell and omics formats such as 10x directories, \code{.h5}, \code{.h5ad},
BPCells directories, and GMT files.

This adapter is exported so \code{rio} can dispatch to it.

This function imports Gene Matrix Transposed (GMT) files into a two-column
data.frame of term–gene pairs.

Adapted from \code{clusterProfiler::read.gmt()} (Y. Yu et al., 2012–2024),
which is licensed under the Artistic License 2.0.
}
\details{
The original implementation is available in the clusterProfiler package:
https://github.com/YuLab-SMU/clusterProfiler
}
\examples{
tmp <- tempfile(fileext = ".csv")
write.csv(mtcars[1:3, 1:3], tmp, row.names = FALSE)
sn_read(tmp)

}
\seealso{
\code{\link[=sn_write]{sn_write()}}

\code{\link[clusterProfiler:reexports]{clusterProfiler::read.gmt()}}
}
\author{
Yu Guangchuang (original implementation)
}
