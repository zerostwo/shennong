% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_tools.R
\name{sn_check_pixi}
\alias{sn_check_pixi}
\alias{sn_install_pixi}
\alias{sn_ensure_pixi}
\title{Check, install, and configure pixi for Shennong Python backends}
\usage{
sn_check_pixi(pixi = NULL, quiet = FALSE)

sn_install_pixi(
  version = "latest",
  pixi_home = "~/.pixi",
  bin_dir = NULL,
  no_path_update = TRUE,
  download_url = NULL,
  force = FALSE,
  quiet = FALSE
)

sn_ensure_pixi(
  pixi = NULL,
  install = TRUE,
  version = "latest",
  pixi_home = "~/.pixi",
  bin_dir = NULL,
  no_path_update = TRUE,
  download_url = NULL,
  quiet = FALSE
)
}
\arguments{
\item{pixi}{Optional pixi executable path. When \code{NULL}, Shennong checks
\code{options("shennong.pixi")}, \code{PATH}, and \code{~/.pixi/bin/pixi}.}

\item{quiet}{Logical; suppress status messages where possible.}

\item{version}{Pixi version passed to the official installer script.
Defaults to \code{"latest"}.}

\item{pixi_home}{Pixi home directory. The installer defaults to
\code{"~/.pixi"}; Shennong runtime workflows set \code{PIXI_HOME} to a
user-level path such as \code{"~/.shennong/pixi/home"}.}

\item{bin_dir}{Optional directory where the standalone pixi binary should be
installed.}

\item{no_path_update}{Logical; when \code{TRUE}, ask the installer not to
edit shell startup files.}

\item{download_url}{Optional custom pixi binary download URL. This is useful
for institutional mirrors.}

\item{force}{Logical; reinstall even if pixi is already available.}

\item{install}{Logical used by \code{sn_ensure_pixi()}; install pixi when it
is not already available.}
}
\value{
\code{sn_check_pixi()} and \code{sn_ensure_pixi()} return a named
list with install status, executable path, and version. \code{sn_install_pixi()}
invisibly returns the refreshed check result.
}
\description{
Shennong uses pixi for optional Python backends such as scVI/scANVI. The R
package ships runner scripts, but the Python environments themselves are
created under the user-level Shennong runtime directory
\code{~/.shennong/}, not inside the current analysis project and not inside
the installed R package.
}
\examples{
info <- sn_check_pixi(quiet = TRUE)
info$installed
\dontrun{
sn_ensure_pixi()
sn_install_pixi(pixi_home = "~/.pixi", no_path_update = TRUE)
}

}
