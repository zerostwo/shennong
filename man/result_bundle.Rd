% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result_bundle.R
\name{result_bundle}
\alias{result_bundle}
\alias{sn_build_result_bundle}
\alias{sn_validate_result_bundle}
\alias{sn_export_result_bundle}
\title{Build, validate, and export a Shennong Result Bundle v1}
\usage{
sn_build_result_bundle(
  result,
  inputs = list(),
  execution = list(),
  artifacts = list(),
  created_at = format(Sys.time(), "\%Y-\%m-\%dT\%H:\%M:\%SZ", tz = "UTC")
)

sn_validate_result_bundle(bundle, error = TRUE)

sn_export_result_bundle(bundle, path, pretty = TRUE, overwrite = FALSE)
}
\arguments{
\item{result}{A canonical Shennong analysis result accepted by
\code{\link[=sn_validate_result]{sn_validate_result()}}.}

\item{inputs}{A list of immutable input-reference records. Each record
requires \code{role}, \code{revision}, at least one of \code{resource_id} or
\code{artifact_id}, and \code{digest = list(algorithm = "sha256", value = "<64 hex characters>")}. Optional fields are \code{media_type},
\code{size_bytes}, and \code{metadata}.}

\item{execution}{A JSON-compatible list of execution provenance such as job,
runtime, image, environment-lock, or source revision identifiers.
Credentials are rejected.}

\item{artifacts}{A list of candidate output-artifact records. Each record
requires \code{role} and a SHA-256 \code{digest}. Optional fields are \code{artifact_id},
bundle-relative \code{path}, \code{media_type}, \code{size_bytes}, and \code{metadata}.}

\item{created_at}{RFC 3339 UTC creation timestamp ending in \code{Z}. By default
the current UTC time is used.}

\item{bundle}{A Result Bundle to validate or export.}

\item{error}{If \code{TRUE}, stop for an invalid bundle. If \code{FALSE}, return a
validation report.}

\item{path}{Destination JSON file.}

\item{pretty}{Write indented JSON.}

\item{overwrite}{Replace an existing destination file.}
}
\value{
\code{sn_build_result_bundle()} returns an \code{sn_result_bundle} list.
\code{sn_validate_result_bundle()} returns a report with \code{valid}, \code{errors}, and
\code{warnings}. \code{sn_export_result_bundle()} invisibly returns the normalized
file path, byte size, and SHA-256 digest of the exported JSON.
}
\description{
A Result Bundle is the package-owned, JSON-compatible handoff boundary
between a validated Shennong analysis result and an external orchestrator.
It contains no service calls or credentials. Input records identify exact
immutable revisions and SHA-256 digests; artifact records describe candidate
outputs that an authorized external service may verify and promote.
}
\examples{
result <- list(
  schema_version = "1.0.0", analysis_type = "demo", name = "example",
  method = "mean", backend = "base", input = list(), parameters = list(),
  tables = list(primary = data.frame(feature = "gene1", value = 1)),
  embeddings = list(), graphs = list(), models = list(), diagnostics = list(),
  warnings = character(),
  provenance = list(
    package_versions = list(Shennong = "0.2.0"),
    random_seed = 1L,
    timestamp = "2026-01-01 UTC"
  )
)
bundle <- sn_build_result_bundle(result)
sn_validate_result_bundle(bundle, error = FALSE)
path <- tempfile(fileext = ".json")
sn_export_result_bundle(bundle, path)
unlink(path)

}
