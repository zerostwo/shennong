% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grn.R
\name{sn_run_pyscenic}
\alias{sn_run_pyscenic}
\title{Run PySCENIC on a Seurat object}
\usage{
sn_run_pyscenic(
  x,
  assay = "RNA",
  layer = "data",
  outdir = "../pyscenic",
  pyscenic = NULL,
  species = "human",
  features = NULL,
  tfs_fname = NULL,
  database_fname = NULL,
  annotations_fname = NULL,
  mode = "custom_multiprocessing",
  ncores = 10,
  seed = 717
)
}
\arguments{
\item{x}{A Seurat object}

\item{assay}{The name of the assay to use for PySCENIC analysis (default is
"RNA")}

\item{outdir}{The output directory for PySCENIC analysis (default is
"./pyscenic")}

\item{pyscenic}{The path to the PySCENIC executable (default is NULL, which
uses the default path)}

\item{species}{The species to use for PySCENIC analysis (default is "human")}

\item{features}{A vector of gene names to use for PySCENIC analysis (default
is NULL, which uses all genes)}

\item{tfs_fname}{The path to the file containing the list of transcription
factors (default is NULL, which uses the default file)}

\item{database_fname}{The path to the file containing the database of motifs
(default is NULL, which uses the default file)}

\item{annotations_fname}{The path to the file containing the annotations of
motifs (default is NULL, which uses the default file)}

\item{mode}{The mode to use for PySCENIC analysis (default is
"custom_multiprocessing")}

\item{ncores}{The number of cores to use for PySCENIC analysis (default is
10)}

\item{seed}{The seed to use for PySCENIC analysis (default is 717)}

\item{slot}{The slot to use for PySCENIC analysis (default is "data")}
}
\value{
A Seurat object with PySCENIC output
}
\description{
This function runs PySCENIC on a Seurat object and generates a new Seurat
object with PySCENIC output.
}
