% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{sn_initialize_seurat_object}
\alias{sn_initialize_seurat_object}
\title{Initialize a Seurat object with optional QC metrics}
\usage{
sn_initialize_seurat_object(
  x,
  metadata = NULL,
  names_field = 1L,
  names_delim = "_",
  project = "Shennong",
  min_cells = 0,
  min_features = 0,
  sample_name = NULL,
  study = NULL,
  species = NULL,
  standardize_gene_symbols = FALSE,
  is_gene_id = FALSE,
  ...
)
}
\arguments{
\item{x}{A matrix, data.frame, sparse matrix, path to counts data, or a
character vector of multiple 10x paths.}

\item{metadata}{Optional metadata (data.frame or similar) to add to the Seurat object.}

\item{names_field}{Passed to \code{SeuratObject::CreateSeuratObject}, indicating how to parse cell names.}

\item{names_delim}{Passed to \code{SeuratObject::CreateSeuratObject}, indicating the delimiter for cell names.}

\item{project}{A project name for the Seurat object.}

\item{min_cells}{Filter out genes expressed in fewer than \code{min_cells} cells.}

\item{min_features}{Filter out cells with fewer than \code{min_features} genes.}

\item{sample_name}{Optional sample name to store in \code{meta.data$sample}.}

\item{study}{Optional study name to store in \code{meta.data$study}.}

\item{species}{Either "human" or "mouse" (case-sensitive). Affects QC metric patterns.}

\item{standardize_gene_symbols}{Logical; standardize gene symbols after object creation.}

\item{is_gene_id}{Logical; treat row names as gene IDs and convert them to symbols when standardizing.}

\item{...}{Additional arguments passed to \code{SeuratObject::CreateSeuratObject()}.}
}
\value{
A \code{Seurat} object with optional QC metadata in its meta.data slot.
}
\description{
This function creates a Seurat object from counts (and optional metadata),
then calculates common QC metrics such as mitochondrial and ribosomal gene
percentages when \code{species} is supplied. Currently supports human and mouse
patterns for these gene sets. When \code{x} points to a 10x Genomics \code{outs}
directory, the function automatically reads the filtered matrix and stores
discovered source metadata such as the raw matrix path and
\code{metrics_summary.csv} contents in \code{Seurat::Misc(object, "input_source")}.
When \code{x} is a character vector of multiple detected 10x paths, such as the
output of \code{\link[=sn_list_10x_paths]{sn_list_10x_paths()}}, the function returns a named list of Seurat
objects and imports each path in one call.
}
\examples{
\dontrun{
# Minimal example:
seurat_obj <- sn_initialize_seurat_object(x = my_counts, project = "ExampleProject")
}
}
