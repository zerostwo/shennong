% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_clustering.R
\name{sn_simulate_scdesign3}
\alias{sn_simulate_scdesign3}
\title{Simulate single-cell counts with scDesign3}
\usage{
sn_simulate_scdesign3(
  object,
  celltype = NULL,
  pseudotime = NULL,
  spatial = NULL,
  other_covariates = NULL,
  ncell = NULL,
  mu_formula = NULL,
  sigma_formula = "1",
  corr_formula = NULL,
  family_use = "nb",
  n_cores = 2,
  assay = "RNA",
  layer = "counts",
  assay_use = "counts",
  return = c("seurat", "sce", "counts", "result"),
  project = "scdesign3",
  combine_original = FALSE,
  seed = 717,
  ...
)
}
\arguments{
\item{object}{A Seurat or SingleCellExperiment object.}

\item{celltype}{Column in \code{colData(object)} or Seurat metadata used as
the cell-type covariate. If \code{NULL}, \code{"seurat_clusters"} is used
when available.}

\item{pseudotime}{Optional pseudotime covariate column name.}

\item{spatial}{Optional length-two vector naming spatial coordinate columns.}

\item{other_covariates}{Optional additional covariate columns.}

\item{ncell}{Number of cells to simulate. Defaults to the input cell count.}

\item{mu_formula, sigma_formula, corr_formula}{scDesign3 model formulas. When
\code{mu_formula} or \code{corr_formula} is \code{NULL}, Shennong builds a
simple default from \code{pseudotime}, \code{spatial}, \code{celltype}, and
\code{other_covariates}.}

\item{family_use}{Marginal distribution passed to scDesign3.}

\item{n_cores}{Number of cores passed to scDesign3.}

\item{assay, layer}{Assay/layer used when \code{object} is a Seurat object.}

\item{assay_use}{Assay name used when \code{object} is already a
SingleCellExperiment. Defaults to \code{"counts"}.}

\item{return}{One of \code{"seurat"}, \code{"sce"}, \code{"counts"}, or
\code{"result"}.}

\item{project}{Project name for returned Seurat objects.}

\item{combine_original}{If \code{TRUE}, return a merged Seurat object
containing original and simulated cells with a \code{simulation_source}
metadata column. Only applies when \code{return = "seurat"} and
\code{object} is a Seurat object.}

\item{seed}{Optional random seed.}

\item{...}{Additional arguments passed to \code{scDesign3::scdesign3()}.}
}
\value{
Simulated data in the requested format.
}
\description{
\code{sn_simulate_scdesign3()} prepares a Seurat or SingleCellExperiment
object for \code{scDesign3::scdesign3()}, chooses simple default formulas
from the supplied covariates, and returns simulated counts as a Seurat
object, SingleCellExperiment, sparse count matrix, or raw scDesign3 result.
Prefer \code{sn_simulate(method = "scdesign3")} for new code.
}
\examples{
\dontrun{
sim <- sn_simulate_scdesign3(
  object = seurat_obj,
  celltype = "cell_type",
  ncell = 1000,
  n_cores = 4
)
}
}
