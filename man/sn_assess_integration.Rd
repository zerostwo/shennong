% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_metrics.R
\name{sn_assess_integration}
\alias{sn_assess_integration}
\title{Assess integration quality across multiple metrics}
\usage{
sn_assess_integration(
  x,
  batch_by = NULL,
  label_by = NULL,
  cluster_by = NULL,
  reduction = .sn_default_metric_reduction(x),
  baseline = NULL,
  baseline_reduction = .sn_default_baseline_reduction(x, reduction),
  graph = NULL,
  dims = NULL,
  k = 20,
  metrics = NULL,
  neighbor_method = c("auto", "graph", "annoy", "exact"),
  max_cells = 5000,
  stratify_by = NULL,
  rare_fraction = 0.02,
  rare_n = 50,
  challenge_threshold = 0.5,
  seed = 717,
  n_trees = 50,
  batch = NULL,
  label = NULL,
  cluster = NULL
)
}
\arguments{
\item{x}{A Seurat object.}

\item{batch_by}{Metadata column containing batch labels.}

\item{label_by}{Optional metadata column containing biological labels such as
cell type annotations.}

\item{cluster_by}{Optional metadata column containing the current clustering.}

\item{reduction}{Reduction name used for the primary integrated embedding.
Defaults to \code{"harmony"} when present, otherwise \code{"pca"}.}

\item{baseline}{Optional Seurat object used as the baseline reference for
PCR batch_by scoring. If \code{NULL}, the baseline is taken from \code{x}.}

\item{baseline_reduction}{Optional reduction name used as the baseline.}

\item{graph}{Optional graph name stored in \code{x@graphs}.}

\item{dims}{Optional integer vector of embedding dimensions to retain.}

\item{k}{Number of neighbors used when a graph must be built from embeddings.}

\item{metrics}{Optional character vector selecting which metrics to compute.
Supported values are \code{"batch_lisi"}, \code{"label_lisi"},
\code{"batch_silhouette"}, \code{"label_silhouette"},
\code{"graph_connectivity"}, \code{"clustering_agreement"},
\code{"isolated_label_score"}, \code{"cluster_purity"},
\code{"cluster_entropy"}, \code{"pcr_batch"}, and
\code{"challenging_groups"}.}

\item{neighbor_method}{Strategy used when a graph must be built. One of
\code{"auto"}, \code{"graph"}, \code{"annoy"}, or \code{"exact"}.}

\item{max_cells}{Optional integer cap used to subsample cells before running
the metrics.}

\item{stratify_by}{Optional metadata column used to preserve representation
during subsampling. Defaults to \code{batch}.}

\item{rare_fraction}{Fraction-of-cells threshold for flagging rare groups.}

\item{rare_n}{Absolute cell-count threshold for flagging rare groups.}

\item{challenge_threshold}{Threshold on the derived \code{challenge_score}
used to flag difficult groups.}

\item{seed}{Random seed used when \code{max_cells} triggers subsampling.}

\item{n_trees}{Number of Annoy trees when \code{neighbor_method = "annoy"}.}

\item{batch}{Deprecated alias for \code{batch_by}.}

\item{label}{Deprecated alias for \code{label_by}.}

\item{cluster}{Deprecated alias for \code{cluster_by}.}
}
\value{
A list with four top-level elements:
\itemize{
\item \code{summary}: one row per summary metric, plus aggregate scores.
\item \code{per_cell}: per-cell outputs such as LISI and silhouette.
\item \code{per_group}: per-group outputs such as connectivity,
composition, and difficult-group diagnostics.
\item \code{parameters}: the effective runtime configuration.
}
}
\description{
This wrapper combines fast, broadly useful metrics for batch_by mixing,
biological conservation, and cluster-level diagnostics. It reuses existing
neighbor graphs when available and otherwise builds a kNN graph with Annoy or
an exact fallback.
}
\examples{
\dontrun{
data("pbmc_small", package = "Shennong")
pbmc <- sn_run_cluster(
  pbmc_small,
  batch_by = "sample",
  species = "human",
  verbose = FALSE
)
metrics <- sn_assess_integration(
  pbmc,
  batch_by = "sample",
  cluster_by = "seurat_clusters",
  reduction = "harmony",
  baseline_reduction = "pca"
)
metrics$summary
}

}
