% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_registry.R
\name{sn_plot_result}
\alias{sn_plot_result}
\title{Plot a stored or direct Shennong analysis result}
\usage{
sn_plot_result(
  object,
  analysis_type = NULL,
  result_id = NULL,
  view = NULL,
  ...
)
}
\arguments{
\item{object}{A Seurat object with stored results, a direct Shennong result,
or a compatible table/legacy assessment supported by the chosen analysis
type.}

\item{analysis_type}{Optional analysis type. Required for ambiguous Seurat
stores and for table/legacy inputs. See \code{\link[=sn_list_plot_methods]{sn_list_plot_methods()}}.}

\item{result_id}{Optional stored result identifier. A unique result or one
named \code{"default"} is selected automatically when omitted.}

\item{view}{Optional registered plot view. The analysis-specific default is
used when omitted.}

\item{...}{View-specific parameters forwarded to the specialized plotter.}
}
\value{
Usually a \code{ggplot} object; heatmap or multi-panel backends may return
a compatible composed plot object.
}
\description{
\code{sn_plot_result()} is the canonical result-aware visualization entry point.
It uses one stable interface across analysis domains: \code{object},
\code{analysis_type}, \code{result_id}, and \code{view}. Existing domain-specific
\verb{sn_plot_*()} functions remain available as compatibility and advanced
interfaces.
}
\details{
A direct Shennong result supplies its own \code{analysis_type}. For Seurat input,
the function retrieves a stored result. If \code{analysis_type} or \code{result_id} is
omitted, it is selected only when unambiguous; a stored result named
\code{"default"} is preferred within a selected analysis type.
}
\examples{
de_table <- data.frame(
  gene = c("G1", "G2", "G3"),
  log2_fold_change = c(2, -1.5, 0.2),
  adjusted_p_value = c(0.01, 0.03, 0.8)
)
sn_plot_result(de_table, analysis_type = "de", view = "volcano")
\dontrun{
sn_list_results(seurat_object, type = "de")
sn_plot_result(
  seurat_object,
  analysis_type = "de",
  result_id = "cluster_markers",
  view = "volcano"
)
}
}
