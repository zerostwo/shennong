% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_examples.R
\name{sn_list_datasets}
\alias{sn_list_datasets}
\title{List datasets available through Shennong}
\usage{
sn_list_datasets(
  source = c("public", "examples", "all"),
  record_id = NULL,
  save_dir = "~/.shennong/data",
  token = NULL,
  overwrite = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{source}{Which registry to list. \code{"public"} lists the current
Shennong public Zenodo collection; \code{"examples"} lists the bundled
PBMC examples; \code{"all"} returns both.}

\item{record_id}{Zenodo record ID for the public Shennong collection.
Defaults to option \code{shennong.public_data_record} or \code{"20044788"}.}

\item{save_dir}{Local cache directory used to store \code{shennong_index.json}.}

\item{token}{Optional Zenodo access token for restricted/private records.}

\item{overwrite}{Logical; if \code{TRUE}, re-download the public index.}

\item{quiet}{Logical; if \code{TRUE}, suppress Zenodo download progress.}
}
\value{
A data frame with one row per loadable sample-level dataset.
}
\description{
\code{sn_list_datasets()} returns a sample-level registry for datasets that can be
loaded with \code{\link{sn_load_data}}. By default it reads the current
Shennong public Zenodo collection, whose machine-readable index follows the
\code{UPLOAD_RULES.md} layout stored in the record.
}
\examples{
\dontrun{
datasets <- sn_list_datasets()
head(datasets[, c("dataset", "study_id", "organism", "technology")])
}

}
