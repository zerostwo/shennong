% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_enrichment.R
\name{sn_run_enrichment}
\alias{sn_run_enrichment}
\title{Run gene set enrichment analysis}
\usage{
sn_run_enrichment(
  x,
  gene_clusters = NULL,
  analysis = NULL,
  species = NULL,
  database = "GOBP",
  collection = NULL,
  subcollection = NULL,
  pvalue_cutoff = 0.05,
  p_adjust_method = "BH",
  qvalue_cutoff = 0.2,
  universe = NULL,
  min_gs_size = 10,
  max_gs_size = 500,
  gsea_exponent = 1,
  duplicate_gene_method = c("error", "max_abs", "max", "mean"),
  result_id = "default",
  source_de_result_id = NULL,
  return_object = inherits(x, "Seurat"),
  prefix = NULL,
  outdir = NULL,
  object = NULL,
  de_p_adjusted_cutoff = 0.05,
  de_logfc_threshold = 0,
  de_direction = c("up", "down", "both")
)
}
\arguments{
\item{x}{A character vector of genes, a named numeric vector for GSEA, a
data frame, or a \code{Seurat} object when enriching a stored DE result.}

\item{gene_clusters}{Optional two-sided formula describing the gene column
and the grouping/ranking column. Examples include \code{gene ~ cluster} for
grouped ORA and \code{gene ~ log2fc} for one global GSEA ranking. The
clusterProfiler grouped-GSEA formula \code{gene | score ~ group} is not yet
supported and fails explicitly rather than being treated as global GSEA.}

\item{analysis}{Optional explicit analysis mode. Named numeric vectors infer
GSEA and character/categorical inputs infer ORA. Supply \code{analysis}
explicitly for a numeric formula RHS because numeric group codes and GSEA
ranking statistics are otherwise ambiguous.}

\item{species}{One of \code{"human"} or \code{"mouse"}.}

\item{database}{One or more databases. Supported values include GO/KEGG
databases such as \code{"GOBP"} and MSigDB collections such as
\code{"H"}, \code{"C2"}, or \code{"C2:CP:REACTOME"}.}

\item{collection}{Optional MSigDB collection used when
\code{database = "MSIGDB"}.}

\item{subcollection}{Optional MSigDB subcollection used when
\code{database = "MSIGDB"} or when you want to override the parsed
subcollection for a collection-level request such as \code{"C2"}.}

\item{pvalue_cutoff}{Cutoff passed unchanged to clusterProfiler. ORA applies
the upstream raw-p, adjusted-p, and q-value reporting rules. GSEA cutoff
behavior is defined by the validated clusterProfiler/enrichit version and
is therefore recorded in the conformance contract.}

\item{p_adjust_method}{Multiple-testing adjustment method passed as
\code{pAdjustMethod}.}

\item{qvalue_cutoff}{ORA q-value cutoff passed as \code{qvalueCutoff}. It is not
used by GSEA.}

\item{universe}{Optional ORA background gene universe in the same symbol
namespace as \code{x}. Stored-DE ORA defaults to the source result's recorded
\code{input$tested_features}; only legacy results without that field fall
back to the source assay feature space. For KEGG the universe is converted
to ENTREZID together with the query genes. Supplying a universe for GSEA is
an error.}

\item{min_gs_size, max_gs_size}{Minimum and maximum tested gene-set sizes.}

\item{gsea_exponent}{GSEA running-score exponent. For reproducible stochastic
GSEA results with clusterProfiler 4.20, call \code{set.seed()} immediately before
\code{sn_run_enrichment()}, as for the direct upstream call.}

\item{duplicate_gene_method}{Policy for duplicate identifiers in a GSEA
ranked list. The default, \code{"error"}, avoids silent changes. Explicit
alternatives are \code{"max_abs"}, \code{"max"}, and \code{"mean"}.}

\item{result_id}{Name used when storing the enrichment result on a Seurat
object. When multiple databases are requested, the database label is
appended automatically unless a vector of names is supplied.}

\item{source_de_result_id}{Optional stored DE-result name associated with the
enrichment input.}

\item{return_object}{Logical; when \code{TRUE} and a Seurat object is
available, return the updated Seurat object instead of raw enrichment
results.}

\item{prefix}{Optional filename prefix when writing results.}

\item{outdir}{Optional output directory. If supplied, each enrichment result
is saved as an \code{.rds} file.}

\item{object}{Alias for \code{x}; supply only one of \code{x} and \code{object}.}

\item{de_p_adjusted_cutoff, de_logfc_threshold}{Significance and absolute
effect thresholds applied when ORA consumes a stored DE result.}

\item{de_direction}{Direction retained from a stored DE result for ORA:
\code{"up"}, \code{"down"}, or an explicit \code{"both"}.}
}
\value{
A single \code{clusterProfiler} result, a named list of results when
multiple databases are requested, or a \code{Seurat} object when
\code{return_object = TRUE}.
}
\description{
Runs GO, KEGG, or MSigDB enrichment using \pkg{clusterProfiler}. It supports
both over-representation analysis (ORA) and ranked-list GSEA. The enrichment
input can be a gene vector, a ranked numeric vector, a data frame, or a
Seurat object paired with \code{source_de_result_id} to reuse stored DE results.
}
\examples{
\dontrun{
sn_run_enrichment(
  x = c("CD3D", "IL7R", "LTB"),
  species = "human",
  database = c("GOBP", "H")
)
}

}
