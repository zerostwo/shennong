% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_metrics.R
\name{sn_calculate_isolated_label_score}
\alias{sn_calculate_isolated_label_score}
\title{Calculate isolated-label preservation scores}
\usage{
sn_calculate_isolated_label_score(
  x,
  label_by = NULL,
  reduction = .sn_default_metric_reduction(x),
  dims = NULL,
  cells = NULL,
  max_cells = 3000,
  stratify_by = NULL,
  isolated_fraction = 0.05,
  isolated_n = 100,
  seed = 717
)
}
\arguments{
\item{x}{A Seurat object.}

\item{label_by}{Metadata column containing biological labels.}

\item{reduction}{Reduction name used to extract embeddings. Defaults to
\code{"harmony"} when present, otherwise \code{"pca"}.}

\item{dims}{Optional integer vector of embedding dimensions to retain.}

\item{cells}{Optional character vector of cell names to score.}

\item{max_cells}{Optional integer cap used to subsample cells before running
the metric. Defaults to \code{3000} because silhouette needs a full
distance matrix.}

\item{stratify_by}{Optional metadata column used to preserve representation
during subsampling. Defaults to \code{label_by}.}

\item{isolated_fraction}{Fraction-of-cells threshold used to flag isolated
labels.}

\item{isolated_n}{Absolute cell-count threshold used to flag isolated labels.}

\item{seed}{Random seed used when \code{max_cells} triggers subsampling.}
}
\value{
A data frame with one row per label_by and columns describing label
abundance, silhouette separation, and whether the label_by is considered
isolated. The attributes \code{overall_score} and \code{isolated_labels}
summarize the isolated-label subset.
}
\description{
This helper focuses on rare or low-frequency labels and summarizes how well
they remain separated in the selected embedding. The score is based on the
mean silhouette width of isolated labels and is scaled to \code{[0, 1]} where
larger values indicate better preservation.
}
\examples{
\dontrun{
data("pbmc_small", package = "Shennong")
pbmc <- sn_run_cluster(
  pbmc_small,
  batch = "sample",
  species = "human",
  verbose = FALSE
)
isolated_tbl <- sn_calculate_isolated_label_score(
  pbmc,
  label_by = "seurat_clusters",
  reduction = "harmony"
)
isolated_tbl
}

}
