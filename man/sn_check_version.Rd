% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_tools.R
\name{sn_check_version}
\alias{sn_check_version}
\title{Check whether Shennong is up to date}
\usage{
sn_check_version(
  channel = c("auto", "cran", "github"),
  package = "Shennong",
  source = "zerostwo/shennong",
  ref = "main",
  repos = getOption("repos"),
  quiet = FALSE
)
}
\arguments{
\item{channel}{One of \code{"auto"}, \code{"cran"}, or \code{"github"}.}

\item{package}{Package name to check. Defaults to \code{"Shennong"}.}

\item{source}{GitHub repository in \code{"owner/repo"} format when checking
the development channel.}

\item{ref}{GitHub ref to inspect. Defaults to \code{"main"}.}

\item{repos}{CRAN-like repositories used for version lookup.}

\item{quiet}{Logical; if \code{TRUE}, suppress the summary message.}
}
\value{
A named list with the installed version, remote version, selected
channel, whether the package is up to date, and the recommended install
command.
}
\description{
This helper compares the installed package version against the latest
available CRAN or GitHub development version. When \code{channel = "auto"}, it
prefers CRAN if a release exists there and otherwise falls back to GitHub.
}
\examples{
\dontrun{
sn_check_version()
sn_check_version(channel = "github")
}

}
