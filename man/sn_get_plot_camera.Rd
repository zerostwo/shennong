% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_embedding.R
\name{sn_get_plot_camera}
\alias{sn_get_plot_camera}
\title{Retrieve a reproducible 3D embedding camera}
\usage{
sn_get_plot_camera(x = NULL)
}
\arguments{
\item{x}{A JSON file, JSON string, camera list, or styled embedding plot/widget.}
}
\value{
A list with azimuth, elevation and roll in degrees, positive zoom,
and two-element pan in normalized screen coordinates. Angles use an
orthographic projection shared by both renderers.
}
\description{
Read the JSON downloaded by an interactive embedding, a camera list, or the
initial camera attached to a static plot/widget. A standalone browser cannot
mutate an R session: use its Download camera or Copy R camera button after
adjusting the view, then pass the returned value to \code{camera} in either plotter.
}
\examples{
camera <- sn_get_plot_camera(list(azimuth = 45, elevation = 20))
camera
}
