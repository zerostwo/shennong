% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_metrics.R
\name{sn_calculate_silhouette}
\alias{sn_calculate_silhouette}
\title{Calculate silhouette widths from a Seurat embedding}
\usage{
sn_calculate_silhouette(
  x,
  label,
  reduction = "pca",
  dims = NULL,
  cells = NULL,
  max_cells = 3000,
  stratify_by = label,
  seed = 717
)
}
\arguments{
\item{x}{A Seurat object.}

\item{label}{Metadata column used as the grouping label.}

\item{reduction}{Reduction name used to extract embeddings. Defaults to
\code{"pca"}.}

\item{dims}{Optional integer vector of embedding dimensions to retain.}

\item{cells}{Optional character vector of cell names to score.}

\item{max_cells}{Optional integer cap used to subsample cells before running
the metric. Defaults to \code{3000} because silhouette needs a full
distance matrix.}

\item{stratify_by}{Optional metadata column used to preserve representation
during subsampling. Defaults to \code{label}.}

\item{seed}{Random seed used when \code{max_cells} triggers subsampling.}
}
\value{
A data frame with per-cell silhouette widths.
}
\description{
Silhouette widths summarize how well cells are separated by a categorical
metadata label in the selected embedding.
}
\examples{
\dontrun{
data("pbmc_small", package = "Shennong")
pbmc <- sn_run_cluster(
  pbmc_small,
  batch = "sample",
  species = "human",
  verbose = FALSE
)
sil_tbl <- sn_calculate_silhouette(
  pbmc,
  label = "seurat_clusters",
  reduction = "harmony"
)
head(sil_tbl)
}

}
