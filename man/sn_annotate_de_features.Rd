% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_annotation.R
\name{sn_annotate_de_features}
\alias{sn_annotate_de_features}
\title{Annotate DE or marker genes by feature class}
\usage{
sn_annotate_de_features(
  x,
  source_result_id = "default",
  species = NULL,
  gene_col = "gene",
  feature_classes = NULL,
  resource = c("auto", "msigdbr", "custom"),
  custom_resource = NULL,
  result_id = NULL,
  return_object = inherits(x, "Seurat")
)
}
\arguments{
\item{x}{A DE/marker data frame, or a Seurat object containing a stored DE
result.}

\item{source_result_id}{Identifier of the stored DE result when \code{x} is a
Seurat object.}

\item{species}{One of \code{"human"} or \code{"mouse"}. When \code{x} is a Seurat object
and \code{species} is \code{NULL}, Shennong tries \code{sn_get_species(x)}.}

\item{gene_col}{Column containing gene symbols in the DE table.}

\item{feature_classes}{Feature classes to annotate. Defaults to
\code{"transcription_factor"}, \code{"surface_membrane"}, \code{"cytokine"}, and
\code{"chemokine"}.}

\item{resource}{Annotation resource. \code{"msigdbr"} uses bundled MSigDB gene
sets through \pkg{msigdbr}; \code{"custom"} uses \code{custom_resource}; \code{"auto"}
chooses \code{"custom"} when \code{custom_resource} is supplied and otherwise
\code{"msigdbr"}.}

\item{custom_resource}{Optional data frame with at least \code{gene} and
\code{feature_class} columns. Optional columns are \code{species},
\code{feature_class_label}, \code{feature_class_term}, and
\code{feature_class_source}.}

\item{result_id}{Optional stored DE result name for the annotated table
when \code{x} is a Seurat object and \code{return_object = TRUE}. Defaults to
\code{paste0(source_result_id, "_feature_classes")}.}

\item{return_object}{If \code{TRUE}, return the updated Seurat object with the
annotated table stored in the canonical Shennong result registry.
Otherwise return the annotated table.}
}
\value{
A tibble with feature-class columns, or an updated Seurat object.
}
\description{
\code{sn_annotate_de_features()} flags genes from marker or differential
expression tables that encode transcription factors, cell-surface or
plasma-membrane proteins, cytokines, or chemokines. It can annotate a direct
data frame or a stored Shennong DE result under
\code{object@misc$shennong$results$de[[source_result_id]]}.
}
\details{
By default the feature classes are derived from MSigDB GO sets through the
optional \pkg{msigdbr} package. For stricter project-specific definitions,
supply \code{resource = "custom"} with a table containing \code{gene} and
\code{feature_class} columns.
}
\examples{
marker_tbl <- tibble::tibble(
  gene = c("TBX21", "CXCL10", "IL7R", "ACTB"),
  cluster = c("Tcell", "Myeloid", "Tcell", "Bcell"),
  avg_log2FC = c(2.1, 1.8, 1.2, 0.4)
)
custom_classes <- tibble::tibble(
  gene = c("TBX21", "CXCL10", "IL7R"),
  feature_class = c("transcription_factor", "chemokine", "surface_membrane")
)
sn_annotate_de_features(
  marker_tbl,
  species = "human",
  resource = "custom",
  custom_resource = custom_classes
)

\dontrun{
obj <- sn_find_de(obj, analysis = "markers", group_by = "cell_type",
  result_id = "celltype_markers", return_object = TRUE
)
obj <- sn_annotate_de_features(obj, source_result_id = "celltype_markers")
sn_get_de_result(obj, result_id = "celltype_markers_feature_classes")
}
}
