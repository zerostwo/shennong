% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage_tracking.R
\name{sn_flush_usage_tracking}
\alias{sn_flush_usage_tracking}
\title{Flush the local usage outbox to a managed remote DBI store}
\usage{
sn_flush_usage_tracking(store, consent, limit = 1000L, strict = TRUE)
}
\arguments{
\item{store}{A \code{backend = "dbi"} store from \code{\link[=sn_create_usage_store]{sn_create_usage_store()}}.}

\item{consent}{Active \code{remote_research} consent from
\code{\link[=sn_confirm_usage_consent]{sn_confirm_usage_consent()}}. It must exactly match the recorded receipt.}

\item{limit}{Maximum completed outbox rows delivered in one call.}

\item{strict}{Stop on a remote error. \code{FALSE} warns and retains the outbox.}
}
\value{
A one-row delivery summary.
}
\description{
Delivery is explicit, consent-gated, and idempotent by \code{session_id} and
\code{run_id}. Only sessions recorded under the exact consent receipt are sent;
other receipts remain pending. Optional remote fields follow the receipt's
data categories. Remote error messages and process identifiers are never
uploaded. A failed flush leaves rows in the local outbox for later retry.
}
