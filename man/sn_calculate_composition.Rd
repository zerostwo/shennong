% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_metrics.R
\name{sn_calculate_composition}
\alias{sn_calculate_composition}
\title{Calculate composition proportions}
\usage{
sn_calculate_composition(
  x,
  group_by,
  variable,
  min_cells = 20,
  measure = c("proportion", "count", "both"),
  additional_cols = NULL,
  sort_by = NULL,
  sort_value = NULL,
  sort_desc = FALSE
)
}
\arguments{
\item{x}{A Seurat object or a data frame.}

\item{group_by}{Column name or character vector of column names used to
define groups.}

\item{variable}{Column name whose proportions should be calculated.}

\item{min_cells}{Minimum number of cells required for a \code{group_by}
category to be retained. Defaults to \code{20}.}

\item{measure}{One of \code{"proportion"}, \code{"count"}, or
\code{"both"}. Defaults to \code{"proportion"}.}

\item{additional_cols}{Optional character vector of additional columns to
carry into the output.}

\item{sort_by}{Optional summary column used to order the primary
\code{group_by} column in the returned table. One of \code{NULL},
\code{"proportion"}, \code{"count"}, or \code{"group_total"}.
Sorting is only applied when \code{group_by} has length 1.}

\item{sort_value}{Optional level of \code{variable} used for sorting when
\code{sort_by} is supplied. Defaults to the first observed level.}

\item{sort_desc}{Logical; if \code{TRUE}, sort in descending order.}
}
\value{
A data frame with per-group proportions.
}
\description{
Calculates the proportion of different categories within groups from metadata.
}
\details{
This function takes either a Seurat object or a data frame (like cell
metadata) and computes the percentage composition of a given \code{variable}
(for example, cell type) for each category specified by \code{group_by}
(for example, sample or condition). When \code{group_by} contains multiple
columns, proportions are calculated within each unique combination of those
grouping columns.
}
\examples{
\dontrun{
composition_df <- sn_calculate_composition(
  x = seu,
  group_by = c("sample_id", "cell_type"),
  variable = "Phase",
  min_cells = 10,
  measure = "both"
)
composition_df <- sn_calculate_composition(
  x = seu,
  group_by = "sample_id",
  variable = "cell_type",
  min_cells = 10
)
print(composition_df)
}

}
