% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_association.R
\name{sn_plot_distribution}
\alias{sn_plot_distribution}
\title{Plot numeric distributions from Seurat metadata, expression, or tables}
\usage{
sn_plot_distribution(
  object,
  features,
  group_by = NULL,
  sample_by = NULL,
  assay = NULL,
  layer = "data",
  view = c("violin", "box", "histogram", "density", "ridge"),
  aggregate_fun = c("mean", "median", "sum"),
  thresholds = list(),
  bins = 30L,
  show_points = FALSE,
  point_alpha = 0.6,
  jitter_width = 0.15,
  palette = "Paired",
  title = NULL,
  x_label = NULL,
  y_label = NULL,
  aspect_ratio = NULL,
  panel_widths = NULL,
  panel_heights = NULL
)
}
\arguments{
\item{object}{A Seurat object or data frame.}

\item{features}{Numeric metadata/expression features or data-frame columns.}

\item{group_by}{Optional metadata/data-frame grouping column.}

\item{sample_by}{Optional biological-sample column used before plotting.}

\item{assay, layer}{Seurat expression source.}

\item{view}{One of \code{"violin"}, \code{"box"}, \code{"histogram"}, \code{"density"}, or
\code{"ridge"}.}

\item{aggregate_fun}{Sample summary when \code{sample_by} is supplied.}

\item{thresholds}{Optional named list of lower/upper reference values for
the requested features.}

\item{bins}{Histogram bins.}

\item{show_points}{Overlay observations on violin or box plots.}

\item{point_alpha, jitter_width}{Point transparency and horizontal jitter.}

\item{palette}{Named Shennong palette or explicit colors.}

\item{title, x_label, y_label}{Optional plot labels.}

\item{aspect_ratio, panel_widths, panel_heights}{Optional panel sizing controls.}
}
\value{
A faceted \code{ggplot} object with source data and figure metadata.
}
\description{
\code{sn_plot_distribution()} consolidates routine violin, box, histogram,
density, ridge, and QC-distribution views under one object-first interface.
When \code{sample_by} is supplied, observations are summarized to biological
samples before plotting.
}
\examples{
values <- data.frame(
  sample = rep(c("S1", "S2"), each = 3),
  condition = rep(c("control", "treated"), each = 3),
  score = c(1, 2, 3, 2, 4, 5)
)
sn_plot_distribution(values, "score", group_by = "condition", view = "box")
}
