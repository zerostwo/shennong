% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_priority.R
\name{sn_run_scissor}
\alias{sn_run_scissor}
\title{Run phenotype-guided Scissor cell selection}
\usage{
sn_run_scissor(
  object,
  bulk_expression,
  bulk_phenotype,
  state_by = NULL,
  sample_by = NULL,
  family = c("binomial", "gaussian", "cox"),
  phenotype = "bulk_phenotype",
  assay = NULL,
  layer = "data",
  store_name = "scissor",
  seed = 717L,
  backend_control = list(),
  return_object = TRUE
)
}
\arguments{
\item{object}{A Seurat object.}

\item{bulk_expression}{Numeric gene-by-bulk-sample expression matrix with
unique gene row names and sample column names.}

\item{bulk_phenotype}{A binary or numeric phenotype vector, or a two-column
time/event matrix for \code{family = "cox"}. Named inputs are aligned strictly
to \code{bulk_expression}; unnamed inputs retain positional compatibility.}

\item{state_by}{Optional cell-state metadata column. When omitted, the
direct result remains cell-first and uses one descriptive \code{all_cells}
state summary.}

\item{sample_by}{Optional biological-sample metadata column.}

\item{family}{Scissor response family.}

\item{phenotype}{Descriptive label stored with the result.}

\item{assay, layer}{Single-cell expression source.}

\item{store_name}{Stored result name under \code{scissor}.}

\item{seed}{Random seed recorded in provenance.}

\item{backend_control}{Direct Scissor controls, or a list containing a
\code{scissor} sub-list. Set \code{reliability = TRUE} to run bootstrap reliability;
set \code{retain_correlation_matrix = TRUE} to retain the long sample-cell
correlation table subject to \code{max_correlation_rows}. Scissor's \code{cutoff} is
an alpha-search stopping criterion rather than a guaranteed upper bound;
Shennong warns and marks the result exploratory when no candidate alpha
achieves it.}

\item{return_object}{Return the updated object instead of the result.}
}
\value{
A Seurat object or unified Scissor state-priority result.
}
\description{
\code{sn_run_scissor()} is the direct Scissor entry point. It returns a cell-first
unified Scissor result while sharing the same backend implementation used by
\code{\link[=sn_prioritize_states]{sn_prioritize_states()}}. The result also carries state and sample summaries,
correlation summaries, model metadata, and optional reliability output.
}
\examples{
\dontrun{
result <- sn_run_scissor(
  object,
  bulk_expression = bulk_matrix,
  bulk_phenotype = bulk_group,
  state_by = "cell_type",
  sample_by = "sample",
  return_object = FALSE
)
result$tables$cells
result$tables$states
}
}
