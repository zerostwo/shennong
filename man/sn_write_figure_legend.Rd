% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretation.R
\name{sn_write_figure_legend}
\alias{sn_write_figure_legend}
\title{Write a figure legend from stored analysis outputs}
\usage{
sn_write_figure_legend(
  object,
  cluster_de_name = NULL,
  enrichment_name = NULL,
  cluster_col = "seurat_clusters",
  background = NULL,
  output_format = c("llm", "human"),
  provider = NULL,
  model = NULL,
  return_prompt = FALSE,
  store_name = "default",
  return_object = TRUE,
  show_progress = interactive(),
  ...
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{cluster_de_name}{Optional stored cluster-marker result.}

\item{enrichment_name}{Optional stored enrichment result.}

\item{cluster_col}{Metadata column containing cluster labels.}

\item{background}{Optional study-specific background information to provide
additional interpretation context.}

\item{output_format}{One of \code{"llm"} for a model-ready prompt bundle or
\code{"human"} for a human-readable summary.}

\item{provider}{Optional model provider function.}

\item{model}{Optional model identifier.}

\item{return_prompt}{If \code{TRUE}, return the prompt bundle without calling
the provider.}

\item{store_name}{Name used under \code{object@misc$interpretation_results}.}

\item{return_object}{If \code{TRUE}, return the updated Seurat object.}

\item{show_progress}{Logical; if \code{TRUE}, emit step-wise progress logs
and, when \pkg{cli} is available, a console progress bar while waiting for
the LLM response.}

\item{...}{Additional arguments forwarded to \code{provider}.}
}
\value{
A prompt bundle, response, or updated \code{Seurat} object.
}
\description{
Write a figure legend from stored analysis outputs
}
\examples{
if (requireNamespace("Seurat", quietly = TRUE)) {
  counts <- matrix(rpois(10 * 24, lambda = 1), nrow = 10, ncol = 24)
  rownames(counts) <- c(
    "CD3D", "CD3E", "TRAC", "LTB", "MS4A1",
    "CD79A", "HLA-DRA", "LYZ", "ACTB", "MALAT1"
  )
  colnames(counts) <- paste0("cell", 1:24)
  obj <- sn_initialize_seurat_object(counts, species = "human")
  obj$cell_type <- rep(c("Tcell", "Bcell"), each = 12)
  Seurat::Idents(obj) <- obj$cell_type
  obj <- Seurat::NormalizeData(obj, verbose = FALSE)
  obj <- sn_find_de(obj, analysis = "markers", group_by = "cell_type",
    layer = "data", min_pct = 0, logfc_threshold = 0,
    store_name = "celltype_markers", return_object = TRUE, verbose = FALSE
  )
  prompt <- sn_write_figure_legend(
    obj,
    cluster_de_name = "celltype_markers",
    cluster_col = "cell_type",
    return_prompt = TRUE
  )
  prompt$task
}
}
