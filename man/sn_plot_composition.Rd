% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_composition.R
\name{sn_plot_composition}
\alias{sn_plot_composition}
\title{Plot metadata-driven composition summaries}
\usage{
sn_plot_composition(
  data,
  x = NULL,
  y = NULL,
  fill = NULL,
  type = "bar",
  data_kind = c("auto", "metadata", "summary"),
  sample_by = NULL,
  unit_by = NULL,
  flow_by = NULL,
  min_cells = 0,
  position = c("stack", "fill", "dodge"),
  summary_fun = c("mean", "median"),
  errorbar = c("se", "sd", "none"),
  show_points = TRUE,
  point_alpha = 0.7,
  jitter_width = 0.15,
  bins = 30,
  alluvium_alpha = 0.8,
  show_stratum_labels = TRUE,
  facet_row = NULL,
  facet_col = NULL,
  order_by = NULL,
  order_value = NULL,
  order_desc = FALSE,
  palette = "Paired",
  angle_x = 45,
  show_legend = TRUE,
  title = NULL,
  x_label = NULL,
  y_label = NULL,
  aspect_ratio = NULL,
  panel_widths = NULL,
  panel_heights = NULL
)
}
\arguments{
\item{data}{A Seurat object or data frame. A data frame may contain cell-level
metadata or a table with a \code{proportion} or \code{count} column.}

\item{x}{Metadata/table column mapped to the x-axis. It may be omitted for an
alluvial plot when \code{flow_by} is supplied.}

\item{y}{Optional value column for summarized input. For metadata input it
must be \code{proportion} or \code{count}; the default is \code{proportion} for filled
bars and sample-level plots, and \code{count} otherwise.}

\item{fill}{Optional composition category or fill column. It is required for
stacked and sample-level composition plots. For alluvial plots it defaults
to the last \code{flow_by} column.}

\item{type}{Plot type: \code{"bar"}, \code{"sample_bar"}, \code{"sample_boxplot"},
\code{"histogram"}, or \code{"alluvial"}. The aliases \code{"stacked_bar"},
\code{"errorbar"}, and \code{"sankey"} are also accepted.}

\item{data_kind}{One of \code{"auto"}, \code{"metadata"}, or \code{"summary"}. In auto
mode, a data frame containing \code{proportion}/\code{count} (or an explicit \code{y}) is
treated as summarized; Seurat input is always metadata.}

\item{sample_by}{Metadata column defining biological samples for
\code{sample_bar} and \code{sample_boxplot}.}

\item{unit_by}{Optional unique unit for \code{bar}, \code{histogram}, or \code{alluvial}
plots, for example a donor ID repeated across cells. Defaults to
\code{sample_by} outside sample-level plot types.}

\item{flow_by}{Character vector of two or more metadata columns forming the
alluvial axes, for example \code{c("level1", "level2", "level3")}.}

\item{min_cells}{Minimum total number of cells required per biological sample
for sample-level plots. For an ordinary metadata bar, it is the minimum
count required for a returned x/fill combination.}

\item{position}{Bar position: \code{"stack"}, \code{"fill"}, or \code{"dodge"}.}

\item{summary_fun}{Center shown by \code{sample_bar}: \code{"mean"} or \code{"median"}.}

\item{errorbar}{Error interval for \code{sample_bar}: \code{"none"}, \code{"sd"}, or
\code{"se"}.}

\item{show_points}{Logical; overlay sample-level observations on a summary
bar or boxplot.}

\item{point_alpha, jitter_width}{Point transparency and horizontal jitter.}

\item{bins}{Number of bins used by \code{histogram}.}

\item{alluvium_alpha}{Transparency of alluvial flows.}

\item{show_stratum_labels}{Logical; label alluvial strata.}

\item{facet_row, facet_col}{Optional metadata/table columns used for faceting.}

\item{order_by}{Optional value column used to reorder the x-axis.}

\item{order_value}{Optional \code{fill} level used when ordering the x-axis.}

\item{order_desc}{Logical; order x levels in descending order.}

\item{palette}{Named Shennong palette or an explicit color vector.}

\item{angle_x}{Rotation angle for x-axis labels.}

\item{show_legend}{Logical; show the legend.}

\item{title, x_label, y_label}{Optional plot labels.}

\item{aspect_ratio}{Optional panel aspect ratio.}

\item{panel_widths, panel_heights}{Optional panel dimensions forwarded to
\code{catplot::theme_cat()} when available.}
}
\value{
A ggplot object with its plot-ready source table attached to the
Shennong figure specification.
}
\description{
\code{sn_plot_composition()} accepts a Seurat object, cell-level metadata, or an
already summarized composition table. It can draw stacked counts or
proportions, sample-level replicate summaries, donor/sample distributions,
and two- or multi-stage alluvial (Sankey-style) diagrams. For Seurat input,
all variables are read from \code{object[[]]} and expression matrices are not
accessed.
}
\details{
Sample-level plots first calculate each biological sample's composition and
only then summarize across conditions. This keeps samples, rather than cells,
as the visual replicate and inserts zeroes for cell types absent from an
otherwise retained sample.
}
\examples{
metadata <- data.frame(
  sample = rep(paste0("S", 1:4), each = 20),
  condition = rep(c("Control", "Treated"), each = 40),
  cell_type = rep(c("T", "B"), 40)
)
sn_plot_composition(metadata, x = sample, fill = cell_type)
sn_plot_composition(
  metadata, x = condition, fill = cell_type,
  type = "sample_boxplot", sample_by = "sample"
)
\dontrun{
sn_plot_composition(
  seu, type = "alluvial",
  flow_by = c("cell_type_level1", "cell_type_level2", "cell_type_level3")
)
}

}
