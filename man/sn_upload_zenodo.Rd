% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_zenodo.R
\name{sn_upload_zenodo}
\alias{sn_upload_zenodo}
\title{Upload reusable data files to Zenodo}
\usage{
sn_upload_zenodo(
  files,
  title,
  description = NULL,
  creators = NULL,
  version = NULL,
  record_id = NULL,
  new_version = FALSE,
  publish = FALSE,
  sandbox = FALSE,
  token = NULL,
  license = "cc-by-4.0",
  resource_type = "dataset",
  access = c("public", "restricted"),
  keywords = NULL,
  manifest = TRUE,
  manifest_dir = tempdir(),
  reserve_doi = TRUE,
  dry_run = FALSE,
  logger = NULL
)
}
\arguments{
\item{files}{Character vector of local files to upload.}

\item{title}{Record title.}

\item{description}{Record description. When \code{NULL}, Shennong writes a
short reusable-data description.}

\item{creators}{Creator names. A character vector such as
\code{c("Duan, Songqi")} is the simplest form. A data frame or list with
\code{name}, \code{firstname}, \code{lastname}, \code{orcid}, and
\code{affiliation}/\code{affiliations} fields is also accepted.}

\item{version}{Dataset version string recorded in Zenodo metadata and the
Shennong manifest.}

\item{record_id}{Optional Zenodo record ID. When supplied,
\code{new_version = FALSE} updates that draft; \code{new_version = TRUE}
creates a new version from the published record.}

\item{new_version}{If \code{TRUE}, create a new Zenodo version for
\code{record_id}.}

\item{publish}{If \code{TRUE}, publish the record after upload. Defaults to
\code{FALSE} so users can inspect the draft first.}

\item{sandbox}{If \code{TRUE}, use the Zenodo sandbox service.}

\item{token}{Zenodo access token. When \code{NULL}, Shennong checks
\code{ZENODO_SANDBOX_TOKEN} for sandbox uploads and then
\code{ZENODO_TOKEN} / \code{ZENODO_PAT}.}

\item{license}{License identifier stored in Zenodo rights metadata.}

\item{resource_type}{Zenodo resource type ID. Defaults to \code{"dataset"}.}

\item{access}{File and record access policy. Defaults to \code{"public"}.}

\item{keywords}{Optional subject keywords.}

\item{manifest}{If \code{TRUE}, upload a
\code{shennong_zenodo_manifest.json} file with checksums and provenance.}

\item{manifest_dir}{Directory used to write the temporary manifest file.}

\item{reserve_doi}{Whether to reserve a DOI for new drafts.}

\item{dry_run}{If \code{TRUE}, build and return the planned record and
manifest without contacting Zenodo.}

\item{logger}{Logger level passed to \code{zen4R::ZenodoManager$new()}.}
}
\value{
A list containing the Zenodo identifiers, uploaded-file manifest,
manifest path, publication status, and the \code{ZenodoRecord} object.
}
\description{
\code{sn_upload_zenodo()} creates or updates a Zenodo draft through
\pkg{zen4R}, uploads user-selected files, and writes a Shennong manifest
alongside those files so downstream users can verify the exact dataset
version and file checksums they reused.
}
\examples{
\dontrun{
result <- sn_upload_zenodo(
  files = c("data/reference_atlas.qs", "data/reference_markers.csv"),
  title = "Reusable PBMC reference atlas",
  creators = "Duan, Songqi",
  version = "2026.05.04",
  sandbox = TRUE
)
result$doi
result$manifest
}

}
