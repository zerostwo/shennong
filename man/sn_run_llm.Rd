% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretation.R
\name{sn_run_llm}
\alias{sn_run_llm}
\title{Run an LLM provider on a prepared message list}
\usage{
sn_run_llm(
  messages,
  provider,
  model = NULL,
  structured_type = NULL,
  tools = NULL,
  ...
)
}
\arguments{
\item{messages}{A message list, typically from \code{sn_build_prompt()}.}

\item{provider}{A user-supplied function that accepts \code{messages} and
returns text or a list containing \code{text}.}

\item{model}{Optional model identifier passed through to the provider.}

\item{structured_type}{Optional structured-output schema or type object
forwarded to providers that support typed responses.}

\item{tools}{Optional list of tool definitions forwarded to providers that
support tool registration or tool calling.}

\item{...}{Additional arguments passed to \code{provider}.}
}
\value{
A list containing at least \code{text}.
}
\description{
Run an LLM provider on a prepared message list
}
\examples{
provider <- function(messages, model = NULL, ...) {
  list(text = paste("received", length(messages), "messages"))
}
sn_run_llm(
  messages = list(list(role = "user", content = "Summarize this result.")),
  provider = provider
)
}
