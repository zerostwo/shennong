% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_integration_benchmark.R
\name{sn_compare_integrations}
\alias{sn_compare_integrations}
\title{Compare integration methods with scib-metrics}
\usage{
sn_compare_integrations(
  object,
  batch_by = NULL,
  label_by,
  methods = NULL,
  assay = "RNA",
  normalized_layer = "data",
  features = NULL,
  max_cells = 100000L,
  accelerator = c("auto", "cpu", "gpu"),
  n_jobs = 1L,
  name = "integration_benchmark",
  return_object = TRUE,
  backend_control = list(),
  seed = 717L,
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object containing \code{misc$integration_comparison}.}

\item{batch_by}{Metadata column containing batch identities.}

\item{label_by}{Metadata column containing biological labels.}

\item{methods}{Optional method names or run IDs to evaluate. A method name
selects all of its registered parameter-grid runs.}

\item{assay}{RNA assay containing normalized expression.}

\item{normalized_layer}{Normalized, unintegrated expression layer used by
scib-metrics for its baseline preparation.}

\item{features}{Optional common features. Defaults to variable features.}

\item{max_cells}{Optional shared stratified cell cap.}

\item{accelerator}{One of \code{"auto"}, \code{"cpu"}, or \code{"gpu"}.}

\item{n_jobs}{Neighbor-search workers.}

\item{name}{Stored result name.}

\item{return_object}{Return the updated Seurat object when \code{TRUE}, otherwise
return the analysis-result object.}

\item{backend_control}{Advanced pixi, runner, metric, and filesystem controls.}

\item{seed}{Sampling seed.}

\item{verbose}{Show backend progress.}
}
\value{
A Seurat object with an \code{integration_benchmark} result, or the result.
}
\description{
Benchmarks the integration reductions registered by a multi-method
\code{\link[=sn_run_cluster]{sn_run_cluster()}} call. UMAP and t-SNE coordinates are never used as metric
inputs. The normalized expression matrix is exported sparsely and every
method is evaluated on the same cells. Parameter-grid objects are grouped by
preprocessing identity, each group uses its matching unintegrated baseline,
and each unique native embedding is scored once before its score is mapped
back to all cluster-resolution runs that share it.
}
