% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage_tracking.R
\name{sn_with_usage_tracking}
\alias{sn_with_usage_tracking}
\title{Evaluate code with temporary local usage tracking}
\usage{
sn_with_usage_tracking(
  expr,
  path = NULL,
  store = NULL,
  consent = NULL,
  mode = c("development", "production", "test", "benchmark"),
  display = FALSE,
  nested = TRUE,
  functions = NULL,
  remote_flush = c("manual", "on_disable"),
  strict = FALSE
)
}
\arguments{
\item{expr}{Code to evaluate.}

\item{path}{Local SQLite file path. It is retained for compatibility and
cannot be combined with \code{store}.}

\item{store}{Optional object from \code{\link[=sn_create_usage_store]{sn_create_usage_store()}}. A remote DBI
store still writes first to its local SQLite outbox.}

\item{consent}{Optional object from \code{\link[=sn_confirm_usage_consent]{sn_confirm_usage_consent()}}. It is
mandatory for a remote store.}

\item{mode}{Explicit execution context: \code{"development"}, \code{"production"},
\code{"test"}, or \code{"benchmark"}. Shennong never infers that a run is production.}

\item{display}{Show a compact elapsed-time message when each tracked call
finishes.}

\item{nested}{Record nested high-level Shennong workflow calls. Summaries
exclude nested calls by default to avoid double-counting elapsed time.}

\item{functions}{Optional exact character vector of exported functions to
instrument. \code{NULL} records every public Shennong and rio adapter except the
usage-tracking control/query functions themselves.}

\item{remote_flush}{For a DBI store, flush manually or when tracking is
disabled. Remote failures on disable warn and retain the local outbox.}

\item{strict}{If \code{TRUE}, a database write failure stops the analysis. The
default is fail-open: warn once and preserve the scientific call.}
}
\value{
The value of \code{expr}, with its visibility preserved.
}
\description{
Evaluate code with temporary local usage tracking
}
