% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_io.R
\name{sn_write}
\alias{sn_write}
\alias{.export.rio_bpcells}
\alias{.export.rio_h5ad}
\alias{.export.rio_h5}
\alias{.export.rio_qs}
\alias{.export.rio_qs2}
\title{Write tabular and bioinformatics file formats}
\usage{
sn_write(
  x,
  path = NULL,
  to = NULL,
  auto_install = TRUE,
  install_repos = getOption("repos"),
  install_ask = FALSE,
  ...
)

.export.rio_bpcells(file, x, overwrite = FALSE, ...)

.export.rio_h5ad(file, x, mode = "w", ...)

.export.rio_h5(file, x, ...)

.export.rio_qs(file, x, ...)

.export.rio_qs2(file, x, ...)
}
\arguments{
\item{x}{Object to write.}

\item{path}{Output path. Missing parent directories are created
automatically before dispatching the selected writer.}

\item{to}{Optional format override when it cannot be inferred from \code{path}.}

\item{auto_install}{Logical; when \code{TRUE}, install missing writer
dependencies before writing. This includes \pkg{rio} plus optional
Shennong custom writer dependencies such as \code{.qs2}, \code{.h5ad},
\code{.h5}, or BPCells. Legacy \code{.qs} output installs \pkg{qs} from
the GitHub remote \code{qsbase/qs} when needed.}

\item{install_repos}{CRAN-like repositories used when \code{auto_install}
needs to install CRAN packages.}

\item{install_ask}{Passed to \code{BiocManager::install()} when
\code{auto_install} installs Bioconductor packages.}

\item{...}{Additional arguments forwarded to the underlying exporter.}

\item{file}{Output path used by the exported \code{rio} adapter methods.}

\item{overwrite}{Logical; overwrite an existing BPCells directory.}

\item{mode}{File mode passed through to \code{anndataR::write_h5ad()}.}
}
\value{
Invisibly returns the output path.
}
\description{
A Shennong wrapper around \code{rio::export()} with support for common
single-cell formats such as BPCells, \code{.h5ad}, and 10x \code{.h5}.

This adapter is exported so \code{rio} can dispatch to it.
}
\examples{
tmp <- tempfile(fileext = ".csv")
sn_write(mtcars[1:3, 1:3], tmp)

}
\seealso{
\code{\link[=sn_read]{sn_read()}}
}
