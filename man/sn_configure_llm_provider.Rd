% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpretation.R
\name{sn_configure_llm_provider}
\alias{sn_configure_llm_provider}
\title{Configure a reusable local LLM provider for Shennong}
\usage{
sn_configure_llm_provider(
  name = "default",
  base_url,
  api_key = NULL,
  api_key_env = "OPENAI_API_KEY",
  provider_type = c("openai_compatible", "openai"),
  wire_api = c("responses", "chat_completions"),
  default_model = NULL,
  review_model = NULL,
  reasoning_effort = NULL,
  backend_preference = c("native", "ellmer", "auto"),
  disable_response_storage = TRUE,
  enable_history = TRUE,
  set_default = TRUE,
  supports_websockets = FALSE,
  requires_openai_auth = TRUE,
  config_dir = "~/.shennong"
)
}
\arguments{
\item{name}{Provider name used in the local registry.}

\item{base_url}{Base URL of the upstream API. A bare host like
\code{"https://api.catplot.org"} is accepted.}

\item{api_key}{API key to store in the local provider config. If omitted,
Shennong will fall back to \code{Sys.getenv(api_key_env)} at runtime.}

\item{api_key_env}{Environment variable name used as a fallback secret source.}

\item{provider_type}{Provider family. Currently \code{"openai_compatible"}
and \code{"openai"} are supported.}

\item{wire_api}{Upstream API style: \code{"responses"} or
\code{"chat_completions"}.}

\item{default_model}{Default model used when no explicit \code{model} is
supplied.}

\item{review_model}{Optional second model name kept for local bookkeeping.}

\item{reasoning_effort}{Optional reasoning level stored alongside the
provider config.}

\item{backend_preference}{Preferred Shennong backend. Use
\code{"native"} for the built-in HTTP client or \code{"ellmer"} for the
optional \pkg{ellmer}-backed adapter.}

\item{disable_response_storage}{Logical; if \code{TRUE}, disable provider-side
response storage when the upstream API supports it.}

\item{enable_history}{Logical; whether configured providers should write call
history into \code{~/.shennong/llm-history/}.}

\item{set_default}{Logical; whether to mark this provider as the default one.}

\item{supports_websockets}{Logical flag stored for user inspection.}

\item{requires_openai_auth}{Logical flag stored for user inspection.}

\item{config_dir}{Local Shennong config root.}
}
\value{
Invisibly returns the updated provider table.
}
\description{
Stores provider metadata under \code{~/.shennong/llm-providers.json} so
Shennong interpretation helpers can reuse a default endpoint without forcing
every call site to pass an explicit provider function.
}
\examples{
cfg_dir <- tempfile("shennong-config-")
sn_configure_llm_provider(
  name = "demo",
  base_url = "https://example.invalid",
  api_key = "demo-key",
  default_model = "gpt-4.1",
  config_dir = cfg_dir
)
sn_list_llm_providers(config_dir = cfg_dir)
}
