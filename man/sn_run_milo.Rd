% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_metrics.R
\name{sn_run_milo}
\alias{sn_run_milo}
\title{Run neighborhood differential abundance testing with miloR}
\usage{
sn_run_milo(
  x,
  sample_by = NULL,
  group_by = NULL,
  contrast = NULL,
  reduction = "pca",
  dims = NULL,
  cells = NULL,
  max_cells = NULL,
  stratify_by = NULL,
  k = 20,
  d = NULL,
  prop = 0.1,
  refined = TRUE,
  refinement_scheme = "reduced_dim",
  covariates = NULL,
  annotation_by = NULL,
  fdr_weighting = c("k-distance", "neighbour-distance", "max", "graph-overlap", "none"),
  min_mean = 0,
  norm_method = c("TMM", "RLE", "logMS"),
  store_name = NULL,
  return_object = FALSE,
  return_intermediate = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{x}{A Seurat object.}

\item{sample_by}{Metadata column defining biological samples.}

\item{group_by}{Metadata column defining the sample-level comparison group.}

\item{contrast}{Optional character vector of length 2 giving the comparison
as \code{c(case, control)}. When omitted, \code{group_by} must contain
exactly two levels in the selected cells.}

\item{reduction}{Reduction name used to build neighborhoods. Defaults to
\code{"pca"}.}

\item{dims}{Optional integer vector of embedding dimensions to retain.}

\item{cells}{Optional character vector of cells to include.}

\item{max_cells}{Optional integer cap used to subsample cells before
neighborhood construction.}

\item{stratify_by}{Optional metadata column used to preserve representation
during subsampling. Defaults to \code{sample_by}.}

\item{k}{Number of neighbors for graph and neighborhood construction.}

\item{d}{Number of embedding dimensions passed to miloR. Defaults to the
selected embedding dimensionality.}

\item{prop}{Proportion of cells sampled as neighborhood indices.}

\item{refined}{Logical; if \code{TRUE}, use miloR's refined neighborhood
sampling.}

\item{refinement_scheme}{Refinement scheme passed to
\code{miloR::makeNhoods()}.}

\item{covariates}{Optional sample-level covariates added to the DA design
formula alongside \code{group_by}.}

\item{annotation_by}{Optional cell-level metadata column used to annotate
neighborhoods with \code{miloR::annotateNhoods()}.}

\item{fdr_weighting}{FDR weighting strategy passed to
\code{miloR::testNhoods()}.}

\item{min_mean}{Minimum mean count threshold passed to
\code{miloR::testNhoods()}.}

\item{norm_method}{Normalization method passed to
\code{miloR::testNhoods()}.}

\item{store_name}{Optional name used under \code{object@misc$milo_results}.
When supplied, the milo result is stored on the Seurat object.}

\item{return_object}{Logical; when \code{TRUE} and \code{store_name} is
supplied, return the updated Seurat object.}

\item{return_intermediate}{Logical; if \code{TRUE}, return a list with the
DA table, design data, and milo object.}

\item{verbose}{Logical; if \code{TRUE}, emit progress logs.}
}
\value{
By default, a data frame of neighborhood-level DA statistics. When
\code{return_intermediate = TRUE}, a list with \code{table},
\code{design_df}, and \code{milo} is returned.
}
\description{
This wrapper runs the standard miloR neighborhood differential abundance
workflow on a Seurat object using one embedding and one sample-level group
contrast. Cells are grouped into neighborhoods, counted per sample, and then
tested for differential abundance between the two sample groups.
}
\examples{
\dontrun{
da_tbl <- sn_run_milo(
  seu,
  sample_by = "sample",
  group_by = "condition",
  contrast = c("treated", "control"),
  reduction = "pca"
)
}

}
