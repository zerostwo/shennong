% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_tools.R
\name{sn_configure_pixi_mirror}
\alias{sn_configure_pixi_mirror}
\title{Configure pixi mirrors for Shennong runtime environments}
\usage{
sn_configure_pixi_mirror(
  mirror = c("default", "auto", "china", "tuna", "ustc", "bfsu"),
  pixi_home = NULL,
  runtime_dir = NULL,
  config_path = NULL,
  append_original = TRUE
)
}
\arguments{
\item{mirror}{One of \code{"default"}, \code{"auto"}, \code{"china"},
\code{"tuna"}, \code{"ustc"}, or \code{"bfsu"}.}

\item{pixi_home}{Pixi home directory. When \code{NULL}, it is derived from
\code{runtime_dir}.}

\item{runtime_dir}{Optional Shennong runtime directory used to derive
\code{pixi_home}.}

\item{config_path}{Optional explicit config path.}

\item{append_original}{Whether to keep the original conda-forge URL as a
fallback after mirror URLs.}
}
\value{
Invisibly returns the config path, or \code{NA_character_} when no
mirror config was written.
}
\description{
Writes a pixi \code{config.toml} under the selected \code{PIXI_HOME}. By
default Shennong uses a user-level pixi home such as
\code{~/.shennong/pixi/home}, so this does not mutate pixi's global
\code{~/.pixi/config.toml} unless \code{pixi_home} points there explicitly.
}
\examples{
tmp <- tempfile("pixi-home-")
sn_configure_pixi_mirror("tuna", pixi_home = tmp)

}
