% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_deconvolution.R
\name{sn_deconvolve_bulk}
\alias{sn_deconvolve_bulk}
\title{Run bulk RNA-seq deconvolution with single-cell references}
\usage{
sn_deconvolve_bulk(
  x,
  bulk,
  method = c("bayesprism", "cibersortx"),
  cell_type_col = NULL,
  cell_state_col = NULL,
  cell_type_labels = NULL,
  cell_state_labels = NULL,
  assay = "RNA",
  layer = "counts",
  bulk_gene_axis = c("auto", "rows", "columns"),
  key = NULL,
  outdir = tempdir(),
  prefix = "deconvolution",
  store_name = "default",
  cibersortx_result = NULL,
  cibersortx_email = NULL,
  cibersortx_token = NULL,
  cibersortx_container = c("docker", "apptainer"),
  cibersortx_container_path = NULL,
  cibersortx_dry_run = FALSE,
  cibersortx_rmbatch_b_mode = FALSE,
  cibersortx_rmbatch_s_mode = FALSE,
  cibersortx_perm = 0,
  cibersortx_qn = FALSE,
  cibersortx_absolute = FALSE,
  cibersortx_abs_method = "sig.score",
  cibersortx_k_max = 999,
  gibbs_control = list(),
  opt_control = list(),
  n_cores = 1,
  update_gibbs = TRUE,
  return_object = TRUE
)
}
\arguments{
\item{x}{A \code{Seurat} object used as the single-cell reference, or a
gene-by-cell matrix-like object.}

\item{bulk}{A bulk expression matrix-like object.}

\item{method}{One of \code{"bayesprism"} or \code{"cibersortx"}.}

\item{cell_type_col}{Metadata column containing cell-type labels when
\code{x} is a \code{Seurat} object. If \code{x} is a matrix, supply
\code{cell_type_labels} instead.}

\item{cell_state_col}{Optional metadata column containing cell-state labels.
Defaults to \code{cell_type_col}.}

\item{cell_type_labels}{Optional vector of cell-type labels for matrix
references.}

\item{cell_state_labels}{Optional vector of cell-state labels for matrix
references.}

\item{assay}{Assay used to extract the single-cell reference matrix.}

\item{layer}{Layer used to extract the single-cell reference matrix.}

\item{bulk_gene_axis}{Orientation of genes in \code{bulk}. Use
\code{"rows"} for the common gene-by-sample layout, \code{"columns"} for
sample-by-gene input, or \code{"auto"} to infer it from the overlap with
the reference genes.}

\item{key}{Optional malignant-cell label passed to BayesPrism.}

\item{outdir}{Output directory used by CIBERSORTx file export.}

\item{prefix}{Prefix used for exported files and stored result names.}

\item{store_name}{Name used under \code{object@misc$deconvolution_results}
when \code{x} is a \code{Seurat} object and a fraction table is available.}

\item{cibersortx_result}{Optional path to a completed CIBERSORTx fractions
result file to import instead of running the local container.}

\item{cibersortx_email}{Optional CIBERSORTx account email.}

\item{cibersortx_token}{Optional CIBERSORTx access token.}

\item{cibersortx_container}{Container runtime used for local execution. One
of \code{"docker"} or \code{"apptainer"}.}

\item{cibersortx_container_path}{Optional Apptainer image path.}

\item{cibersortx_dry_run}{If \code{TRUE}, prepare files and return local
commands without running the container.}

\item{cibersortx_rmbatch_b_mode}{Whether to enable B-mode batch correction.}

\item{cibersortx_rmbatch_s_mode}{Whether to enable S-mode batch correction.}

\item{cibersortx_perm}{Number of permutations used by the fractions module.}

\item{cibersortx_qn}{Whether to enable quantile normalization.}

\item{cibersortx_absolute}{Whether to enable absolute mode.}

\item{cibersortx_abs_method}{CIBERSORTx absolute-mode method.}

\item{cibersortx_k_max}{Maximum condition number used when constructing the
signature matrix.}

\item{gibbs_control}{Optional BayesPrism Gibbs-sampler control list.}

\item{opt_control}{Optional BayesPrism optimization control list.}

\item{n_cores}{Number of cores passed to BayesPrism.}

\item{update_gibbs}{Whether BayesPrism should run the final Gibbs update.}

\item{return_object}{If \code{TRUE} and \code{x} is a \code{Seurat} object,
return the updated object when a result table is available.}
}
\value{
A stored-result list, an export-bundle list, or an updated
\code{Seurat} object depending on the selected backend and
\code{return_object}.
}
\description{
This helper supports two bulk deconvolution workflows that combine a
single-cell reference with bulk RNA-seq mixtures:
}
\details{
\itemize{
\item \code{"bayesprism"} runs the \pkg{BayesPrism} R package locally.
\item \code{"cibersortx"} prepares inputs and runs the local CIBERSORTx
container workflow, or imports an existing fractions result file.
}
}
\examples{
if (requireNamespace("Seurat", quietly = TRUE)) {
  counts <- matrix(rpois(20 * 18, lambda = 3), nrow = 20, ncol = 18)
  rownames(counts) <- paste0("gene", seq_len(20))
  colnames(counts) <- paste0("cell", seq_len(18))
  ref <- sn_initialize_seurat_object(counts, species = "human")
  ref$cell_type <- rep(c("Tcell", "Bcell", "Mono"), each = 6)
  bulk <- cbind(
    sample_a = rowSums(counts[, 1:12, drop = FALSE]),
    sample_b = rowSums(counts[, 7:18, drop = FALSE])
  )
  bundle <- sn_deconvolve_bulk(
    ref,
    bulk = bulk,
    method = "cibersortx",
    cell_type_col = "cell_type",
    outdir = tempdir(),
    cibersortx_email = "demo@example.org",
    cibersortx_token = "fake-token",
    cibersortx_dry_run = TRUE,
    return_object = FALSE
  )
  names(bundle$files)
}

}
