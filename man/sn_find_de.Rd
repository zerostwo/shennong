% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de.R
\name{sn_find_de}
\alias{sn_find_de}
\title{Run differential expression analysis on a Seurat object}
\usage{
sn_find_de(
  object,
  analysis = NULL,
  ident_1 = NULL,
  ident_2 = NULL,
  group_by = NULL,
  subset_by = NULL,
  subset_levels = NULL,
  sample_col = NULL,
  assay = "RNA",
  layer = "counts",
  slot = "data",
  features = NULL,
  test_use = "wilcox",
  pseudobulk_method = c("DESeq2", "edgeR"),
  only_pos = NULL,
  logfc_threshold = 0.1,
  min_pct = 0.25,
  p_val_cutoff = 0.05,
  de_logfc = 0.25,
  min_cells_per_sample = 10,
  store_name = "default",
  return_object = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{analysis}{One of \code{"markers"}, \code{"contrast"}, or
\code{"pseudobulk"}. If \code{NULL}, the function infers the analysis from
the other arguments.}

\item{ident_1, ident_2}{Group labels for direct contrasts. These are required
for \code{"contrast"} and \code{"pseudobulk"} analyses.}

\item{group_by}{Optional metadata column that defines the groups to compare.
When omitted, Seurat identities are used.}

\item{subset_by}{Optional metadata column used to repeat a contrast within
each subset, for example per cell type.}

\item{subset_levels}{Optional character vector of subset values to analyze.
Defaults to all observed values in \code{subset_by}.}

\item{sample_col}{Metadata column containing sample IDs. Required for
\code{"pseudobulk"} analyses.}

\item{assay}{Assay used for DE analysis. Defaults to \code{"RNA"}.}

\item{layer}{Count layer used for pseudobulk aggregation. Defaults to
\code{"counts"}.}

\item{slot}{Seurat slot used for marker/contrast analyses. Defaults to
\code{"data"}.}

\item{features}{Optional feature subset to test.}

\item{test_use}{Statistical test used by Seurat marker/contrast analyses.}

\item{pseudobulk_method}{One of \code{"DESeq2"} or \code{"edgeR"}.}

\item{only_pos}{Whether to return only positive markers. Defaults to
\code{TRUE} for \code{"markers"} and \code{FALSE} otherwise.}

\item{logfc_threshold, min_pct}{Standard Seurat marker filtering arguments.}

\item{p_val_cutoff}{Adjusted p-value threshold used when storing result
metadata.}

\item{de_logfc}{Absolute log fold-change threshold used when storing result
metadata.}

\item{min_cells_per_sample}{Minimum cells required for a sample/group
pseudobulk profile to be retained.}

\item{store_name}{Key used under \code{object@misc$de_results}.}

\item{return_object}{If \code{TRUE}, return the updated Seurat object with
stored DE results. Otherwise return the result table.}

\item{verbose}{Whether to emit progress information.}

\item{...}{Additional arguments passed through to Seurat DE functions.}
}
\value{
Either a DE result table or an updated \code{Seurat} object.
}
\description{
This function provides a single entry point for:
\itemize{
  \item marker discovery across all groups (`Seurat::FindAllMarkers()`),
  \item direct contrasts between two groups (`Seurat::FindMarkers()`), and
  \item pseudobulk contrasts aggregated by sample with `DESeq2` or `edgeR`.
}
}
\details{
Results can be stored back into `object@misc$de_results[[store_name]]`, so
downstream helpers such as `sn_plot_dot()` can reuse the same marker table.
}
