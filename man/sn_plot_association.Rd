% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_association.R
\name{sn_plot_association}
\alias{sn_plot_association}
\title{Plot a numeric association or sample-to-sample expression correlation}
\usage{
sn_plot_association(
  object,
  x,
  y,
  sample_by = NULL,
  group_by = NULL,
  assay = NULL,
  layer = "data",
  aggregate_fun = c("mean", "median", "sum"),
  method = c("spearman", "pearson", "kendall"),
  features = NULL,
  transform = c("none", "log1p"),
  add_fit = TRUE,
  label = FALSE,
  max_points = NULL,
  seed = 717,
  point_size = 1.5,
  point_alpha = 0.7,
  palette = "Paired",
  title = NULL,
  x_label = NULL,
  y_label = NULL,
  aspect_ratio = 1,
  panel_widths = NULL,
  panel_heights = NULL
)
}
\arguments{
\item{object}{A Seurat object, data frame, numeric feature-by-sample matrix,
\code{SummarizedExperiment}, bulk input list, or bulk-QC result containing
\code{tables$expression}.}

\item{x, y}{Numeric metadata/feature names, data-frame columns, or sample
columns for matrix-like input.}

\item{sample_by}{Optional Seurat metadata or data-frame column used to
aggregate cell/observation values to biological samples.}

\item{group_by}{Optional metadata/data-frame column mapped to color. With
\code{sample_by}, it must be constant within each sample.}

\item{assay, layer}{Seurat expression source used when \code{x} or \code{y} is a feature.}

\item{aggregate_fun}{Sample summary: \code{"mean"}, \code{"median"}, or \code{"sum"}.}

\item{method}{Correlation method.}

\item{features}{Optional feature subset for matrix-like input.}

\item{transform}{Optional transformation applied to both numeric axes.}

\item{add_fit}{Add an ordinary least-squares trend line.}

\item{label}{Label observations/features.}

\item{max_points}{Optional deterministic maximum number of displayed points.}

\item{seed}{Seed used when \code{max_points} downsamples observations.}

\item{point_size, point_alpha}{Point size and transparency.}

\item{palette}{Named Shennong palette or explicit colors.}

\item{title, x_label, y_label}{Optional plot labels.}

\item{aspect_ratio, panel_widths, panel_heights}{Optional panel sizing controls.}
}
\value{
A \code{ggplot} object with source data and figure metadata.
}
\description{
This is the canonical scatter/correlation entry point for tabular, Seurat,
and bulk expression inputs. Seurat observations can optionally be aggregated
to biological samples before correlation. For a feature-by-sample matrix,
\code{x} and \code{y} name the two sample columns and each point is a feature.
}
\examples{
expression <- matrix(
  c(3, 5, 8, 4, 6, 9), nrow = 3,
  dimnames = list(c("G1", "G2", "G3"), c("sample_a", "sample_b"))
)
sn_plot_association(expression, x = "sample_a", y = "sample_b")
}
