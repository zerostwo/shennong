% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_io.R
\name{sn_list_input_dirs}
\alias{sn_list_input_dirs}
\title{List 10x-formatted input directories under a root folder}
\usage{
sn_list_input_dirs(path, recursive = TRUE, include_root = TRUE, format = "10x")
}
\arguments{
\item{path}{Root directory to scan.}

\item{recursive}{Logical; if \code{TRUE}, scan subdirectories recursively.
Defaults to \code{TRUE}.}

\item{include_root}{Logical; if \code{TRUE}, also test \code{path} itself.
Defaults to \code{TRUE}.}

\item{format}{One or more directory formats to match. Defaults to
\code{"10x"}. Supported values are \code{"10x"}, \code{"10x_spatial"},
\code{"starsolo"}, and \code{"bpcells"}.}
}
\value{
A character vector of matching directory paths.
}
\description{
This helper scans a root directory and returns the subdirectories that look
like 10x Genomics matrix folders based on the same directory-format
detection used by \code{\link[=sn_read]{sn_read()}}. By default it searches recursively and
returns only standard 10x matrix directories, not STARsolo, BPCells, or
spatial bundles.
}
\examples{
root <- tempfile("tenx-root-")
dir.create(root)
tenx_dir <- file.path(root, "sample1")
dir.create(tenx_dir)
file.create(file.path(tenx_dir, c("matrix.mtx.gz", "barcodes.tsv.gz", "features.tsv.gz")))
sn_list_input_dirs(root)

}
