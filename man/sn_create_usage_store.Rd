% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage_tracking.R
\name{sn_create_usage_store}
\alias{sn_create_usage_store}
\title{Create a local or managed-remote usage store}
\usage{
sn_create_usage_store(
  backend = c("sqlite", "dbi"),
  path,
  connect = NULL,
  table_prefix = "shennong_usage",
  allow_remote_ddl = FALSE
)
}
\arguments{
\item{backend}{\code{"sqlite"} for local-only storage or \code{"dbi"} for a managed
remote DBI destination backed by a local SQLite outbox.}

\item{path}{Local SQLite database/outbox path.}

\item{connect}{For \code{backend = "dbi"}, a zero-argument function returning a
new valid \code{DBIConnection}. Do not return a connection opened before a fork.}

\item{table_prefix}{Remote table prefix.}

\item{allow_remote_ddl}{Permit an explicitly consented flush to create the
two remote tables. Keep \code{FALSE} when migrations are administrator-owned.}
}
\value{
An \code{sn_usage_store} object. It contains a connection factory in
memory and must not be serialized or committed.
}
\description{
A remote DBI store always retains a local SQLite outbox at \code{path}. Scientific
calls write only to the outbox; \code{\link[=sn_flush_usage_tracking]{sn_flush_usage_tracking()}} performs the
explicit, consent-gated remote delivery. The \code{connect} closure and its
credentials are never serialized into the usage database.
}
