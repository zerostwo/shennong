% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_trajectory.R
\name{sn_run_trajectory}
\alias{sn_run_trajectory}
\title{Infer trajectories and test dynamic genes}
\usage{
sn_run_trajectory(
  object,
  method = c("slingshot", "monocle3", "palantir"),
  reduction = NULL,
  cluster_by = NULL,
  start = NULL,
  end = NULL,
  lineages = NULL,
  store_name = "trajectory",
  dims = NULL,
  assay = NULL,
  counts_layer = "counts",
  test_dynamic = TRUE,
  dynamic_features = NULL,
  max_dynamic_features = 2000L,
  nknots = 6L,
  trend_features = NULL,
  trend_points = 100L,
  backend_control = list(),
  return_object = TRUE
)
}
\arguments{
\item{object}{A Seurat object with a dimensional reduction and cluster labels.}

\item{method}{Trajectory backend. Slingshot is currently implemented;
Monocle 3 and Palantir remain discoverable planned backends.}

\item{reduction}{Reduction used for inference. Defaults to PCA, then UMAP or
the first available reduction.}

\item{cluster_by}{Metadata column containing cluster labels. Defaults to
\code{seurat_clusters}, then active identities.}

\item{start, end}{Optional start and terminal cluster labels.}

\item{lineages}{Optional named list of expected cluster paths. Their endpoints
constrain Slingshot and inferred paths are checked against them.}

\item{store_name}{Name used under the \code{trajectory} result type.}

\item{dims}{Reduction dimensions used for inference.}

\item{assay}{Assay used for dynamic-gene counts.}

\item{counts_layer}{Raw-count layer used by tradeSeq.}

\item{test_dynamic}{Fit tradeSeq dynamic and branch tests.}

\item{dynamic_features}{Optional features tested by tradeSeq.}

\item{max_dynamic_features}{Maximum number of dynamic features fitted by default.}

\item{nknots}{Number of tradeSeq spline knots.}

\item{trend_features}{Features for which fitted trends are retained.}

\item{trend_points}{Number of fitted points per lineage and feature.}

\item{backend_control}{Named \code{slingshot} and \code{tradeSeq} argument lists.}

\item{return_object}{Return the updated object instead of the result.}
}
\value{
A Seurat object or a unified trajectory result.
}
\description{
Runs a cluster-aware Slingshot trajectory, stores per-cell pseudotime and
lineage probabilities, and optionally fits tradeSeq negative-binomial GAMs.
The returned result follows the unified Shennong analysis-result contract.
}
\examples{
\dontrun{
object <- sn_run_trajectory(
  object, reduction = "pca", cluster_by = "seurat_clusters",
  start = "0", store_name = "development"
)
result <- sn_get_result(object, "trajectory", "development")
result$tables$cells
}

}
