# Retrieve curated core metabolic signatures

Retrieve curated core metabolic signatures

## Usage

``` r
sn_metabolic_signatures(species = c("human", "mouse"))
```

## Arguments

- species:

  Species label. Matching is case-insensitive, so the same symbols
  support common human and mouse feature conventions.

## Value

A named list of pathway gene vectors.
