# (internal) Evaluate an expression on the unaccelerated upstream code path

(internal) Evaluate an expression on the unaccelerated upstream code
path

## Usage

``` r
.sn_with_acceleration_disabled(expr)
```
